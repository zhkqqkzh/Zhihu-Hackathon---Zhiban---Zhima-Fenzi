# 知伴 · 黑客松冲刺计划书（知识炼金场赛道）

> 本文档聚焦两件事：**① 技术——把 Demo 文章源换成官方「知乎知识内容 API」的真实知乎正文**；**② 创意——把"困惑数据"叙事讲成社区生态的一环**。
> 正式「产品说明」文档（提交必交项）暂不在本文范围，后续单独补。
> 赛道：**📚 知识炼金场**（官方原话：让知识被「理解、吸收、再次创造」）。

---

## 0. 定位与目标

- **一句话定位**：知伴把"读长文卡住"这件每个人都经历过、却没人量化的事，变成可回流的社区数据。
- **本次冲刺的终极检验标准（一句话）**：**评委打开前三秒，产生的第一感觉必须是"这东西真能解决一个我认得的问题"**——而不是"又一个 AI 划词工具"。这是整份计划的最高优先级，所有改动都服务于这一感觉。
- **我们解决的"真实问题"到底是什么**（必须讲清，否则评委无感）：
  - ❌ 不是"查词慢"——那是痒点，没人会为省 3 秒去装一个插件。
  - ✅ 是"**本来想读、读到一半因为卡在一个概念上，就放弃了**"。知乎上有大量优质长文（深度学习、量子、医学），普通读者读到第三段就丢了，这些知识"被写出来却没被吸收"。这正是知识炼金场"**吸收**"环节的断点。
  - 知伴修复的是"**吸收**"：让本来会放弃的人，读完、读懂、留下。
- **本次冲刺两大支柱**：
  1. 让 Demo 跑在**真实知乎知识正文**上，而不是 3 篇手写范文（技术）。
  2. 把核心卖点讲成"**知乎的困惑数据空白**"，而非一个 AI 划词小工具（创意）。
- **不做**（明确排除）：正式产品说明文档（第二部分第 2 点，搁置）；移动端插件落地（形态天花板，非主路径）。

---

## 1. 技术计划：接入官方「知乎知识内容 API」

### 1.1 为什么必须做

| 现状 | 问题 |
|---|---|
| Demo 用 `demo/js/data/articles.js` 里**手写的 3 篇 ML 文章** | 评委上手的是"范文"，不是真知乎长文 → 真实场景缺位 |
| 官方给了**无鉴权**接口，返回真实知乎知识正文 | 不用白不用：真实场景 + 多一个官方能力点 + 完美贴知识炼金场 |

一举三得：① 解决之前诊断的"真实场景缺位"；② 多一个官方能力点（说明里能写"知识内容 API + 搜索"）；③ 命中赛道"把真实知识转化为学习体验"。

### 1.2 接入点（已核对代码）

- **数据源**：`demo/js/data/articles.js` 导出 `ARTICLES` / `ARTICLE_BY_ID`，被 8 个模块消费：`article.js`、`creator.js`、`curation.js`、`guide.js`、`hub.js`、`home.js`、`igloo.js`、`main.js`。
- **预扫描**：`demo/js/core/prescan.js` 的 `cleanPrescanConcepts(text, concepts)` 消费文章正文 `body`（HTML 文本，经 `matchConcepts` 做长词优先匹配）。
- **解释链路**：`demo/js/app/api.js` 的 `POST /ask` 接收 `{ term, context }`，`context` 为划词所在本段语境。
- **官方接口**（无需鉴权，来自 `相关资料/zhihu-skill/.../hackathon-content-api.md`）：
  - 列表：`GET https://api.zhihu.com/km-indep-home/hackathon/v2/knowledge/list`
  - 详情：`GET https://api.zhihu.com/km-indep-home/hackathon/v2/knowledge/{work_id}`
  - 响应含：`work_id`、`title`、`author_name`、`author_avatar`、`introduction`、`content`(正文)、`labels`。

### 1.3 改造方案

1. **新增 `demo/js/data/knowledge-source.js`**
   - `fetchKnowledgeList()`：只访问文档列出的 `api.zhihu.com/km-indep-home/hackathon/v2/knowledge/list`，`Accept: application/json`，**不拼用户输入、不做域名切换**。
   - `fetchKnowledgeDetail(work_id)`：校验 `work_id` 为列表返回的非空标识，拒绝含 `/ ? # 回车` 的值，用语言自带的 URL path 编码函数拼接。
   - 失败/超时：返回 `null`，**不循环重试、不生成假正文**（官方内容边界硬性要求）。

2. **数据归一化**
   - 把官方 `{ work_id, title, author_name, content, introduction, labels }` 映射成现有文章形状 `{ id, title, author, body, intro }`。
   - `body` 兼容 `content` 可能是纯文本或 HTML：做一层**安全清洗**（剥离 `<script>`、禁止 `on*` 事件属性、限制 `javascript:`），保留排版标签供预扫描与高亮。
   - 如实保留 `author_name`、来源归属（官方内容使用边界）。

3. **启动流程接入**
   - `home.js` / `main.js` 加载文章时：优先 `fetchKnowledgeList()` 拿若干真实知识作品 → 取 2–3 篇 fetch 详情 → 拿到 `content` 后**复用现有预扫描 + 划词 + 解释链路**（无需改 prescan / api.js 内部逻辑，只换数据来源）。
   - 预扫描词表在真实正文上可能更杂，需在真实长文上复测幽灵标记率（目标 <5%，沿用 `prescan.js` 的 `cleanPrescanConcepts` 三层过滤）。

4. **降级（硬性，不可省）**
   - 接口失败 / 超时 / 字段缺失 / 活动结束后接口失效 → **自动回退 `articles.js` 内置 3 篇**。
   - 展示真实 HTTP 状态与安全收敛后的错误提示，不循环重试，不生成虚假正文。
   - 理由：该接口是比赛专属，活动后可能失效，**降级保证 Demo 永远不白屏**。

### 1.4 验收标准

- [ ] 联网时，Demo 首页 / 文章页展示的是**真实知乎知识正文**（非内置 3 篇）。
- [ ] 断网或接口 403 / 超时 → 自动回退内置文章，**无白屏、无报错**。
- [ ] 预扫描 / 划词 / 解释在真实正文上仍正常，幽灵标记率 <5%。
- [ ] 真实正文的作者、来源归属如实保留。
- [ ] 接口异常时错误提示真实、收敛，不循环重试。

### 1.5 工作量估计

| 任务 | 估时 |
|---|---|
| 数据源封装 + 归一化 + 安全清洗 | ~0.5 人日 |
| 启动流程接入 + 降级兜底 | ~0.5 人日 |
| 真实正文上的预扫描词表调优 | ~0.5 人日 |
| 联调 + 截图验证（联网/断网两态） | ~0.5 人日 |

---

## 2. 创意叙事方案（想法创意层 —— 评审重点）

> 工程已健康（测试全绿、部署与本地一致）。评审真正比的是**"评委信不信你真解决了问题"**。下面三点是话术资产，不写代码，但决定评分天花板。

### 2.1 核心杀手锏：困惑数据 = 知乎的空白

- **事实**：知乎知道一篇回答有多少**赞、收藏、评论**；但**不知道**读者读到第几段、多少人卡在哪个词上。评论区是赞美和抬杠，不是困惑标记；知乎也没有"这篇里 X 概念我没懂"的按钮。
- **知伴补的正是这个**：把"读者卡住"变成可量化、可回流的数据——这是知乎**没有的新数据类型**。
- **怎么讲**：一张对比图「知乎知道的 vs 知乎不知道的」——右边那栏"读到第几段 / 卡在哪个词"是知伴独有的。
- **为什么命中评分**：它把产品从"又一个 AI 划词工具"升级为"**社区生态的一环**"，直接成立评审表的"与社区生态契合点""对社区用户实际价值"两栏。

### 2.2 卡点飞轮：诚实标注 + 路线图（关键取舍）

- **现状（不藏）**：Demo 的卡点汇总来自 `STUCK_SEED` 预置数据（界面已标"演示环境数据"），`store.js` 明确"仅存本浏览器不上传"，SCF 无 `/stuck` 路由。
- **讲法（把弱点讲成原则）**：
  - **隐私优先是刻意取舍**——单机隐私版先把体验跑通、保护读者数据，这是设计原则，不是没做完。
  - **社区汇总是路线图下一步**——当读者同意共享时，卡点回流到答主与社区，飞轮才闭合。
  - 把"当前单机"和"未来社区"讲成一条**演进路线**，比硬凑假数据体面得多，也诚实。
- **加分动作（若时间允许，呼应此前 #1）**：SCF 加一个最小 `/stuck` 上报 + 聚合，现场用多台设备互见卡点 → 飞轮真转。这不是必须，但做了就是演示现场的最大亮点。

### 2.3 官方能力组合：讲"为什么这么选"

- **用官方**：知乎搜索（站内跳转前置）、知乎知识内容 API（真实文章源，本计划 §1 新增）。
- **不用官方直答、自建 GLM 解释**：点明取舍——
  - 官方直答额度**仅 100 次/日**，撑不住 Demo 高频划词；
  - 你需要的是"**概念 + 本段语境**"的精准流式解释，自建 GLM 可控、可流式、可带前置概念链（如"损失函数 → 梯度下降"）。
- **要点**：主动讲清"为什么用 A 不用 B"，体现**真懂官方能力**，而非为用而用。这比罗列"我用了 X 个 API"更有说服力。

### 2.4 一句话主线（贴知识炼金场原话）

> **理解**（划词 + 本段语境解释）→ **吸收**（卡点标记告诉你缺什么）→ **再次创造**（答主视角补前置说明）

整段演示、文档、视频都围绕这条线，不要散。

---

## 3. 提交物料清单（对齐官方提交检查）

| 物料 | 状态 | 备注 |
|---|---|---|
| 线上 Demo（公网可访问） | ✅ 已有 | `z.toply.top/zhiban-demo/`，9/15 前确认始终可开 |
| 赛道选择 | ⏳ 提交时选 | 务必选 **📚 知识炼金场** |
| 用了哪些知乎开放能力 | ⏳ 本计划新增后 | 搜索 + 知识内容 API（§1） |
| 演示视频（加分） | ❌ 待录 | 30–60s，走"划词解释 → 卡点标记 → 答主视角"主线 |
| Gitee 仓库 public + README 注官方能力 | ⏳ | 当前 README 已记 Demo 地址 |
| 正式产品说明文档 | ⏳ 搁置 | 第二部分第 2 点，后续补（本计划书可作雏形） |

---

## 4. 时间线（距 9/15 提交约 3 天的紧凑版；更大胆的规划见 §6.7 阶段蓝图）

> §6.7 是按"第一感觉成立度"排的阶段路线，不受时间约束。下面这版是**保住提交 deadline** 的最小可执行排期，与 §6.7 的 P1/P2/P4 对应。

- **Day 1**：§1 接入官方知识内容 API（封装 + 归一化 + 降级），跑通真实正文加载（对应 P1）。
- **Day 2**：§6.2 首屏痛点重构（方案 A）+ §6.3 演示剧本走通 + 真实正文预扫描调优；录制演示视频（对应 P2/P4）。
- **Day 3**：全链路回归测试（check/core/hub/api/e2e 全绿）+ 提交 + 确认赛道选「知识炼金场」+ 演示彩排（按 §6.8）。

---

## 5. 风险与兜底

| 风险 | 兜底 |
|---|---|
| 官方知识内容 API 活动后失效 / 比赛当天抽风 | §1.4 降级回内置 3 篇，Demo 永不白屏 |
| 真实正文预扫描词表失控（幽灵/通用词） | 沿用 `prescan.js` 三层过滤；真实长文复测 <5% |
| 卡点飞轮被评委追问"数据是真的吗" | §2.2 话术：诚实标演示数据 + 隐私优先 + 路线图 |
| 直答额度耗尽 | 解释用自建 GLM，不依赖官方直答 |

---

## 6. 体验蓝图：让评委第一秒相信"这真能解决真实问题"

> 本节是整份计划的灵魂。技术（§1）和叙事（§2）都为了服务这一节：**让打开 Demo 的人，在三秒内代入"我也有过这种经历"，并亲眼看到问题被解决**。
> 不谈时间，只谈"做到什么程度，第一感觉才成立"。分四层：首屏、剧本、数据、代入。

### 6.1 设计目标：第一感觉的判据

评委（或任何访客）打开后，内心独白应按这个顺序走：

1. **"这说的就是我"** —— 首屏第一眼是每个人都懂的痛点，不是功能列表。
2. **"原来是这个断了"** —— 立刻明白缺的不是查词速度，是"读着读着就放弃了"。
3. **"它真解决了"** —— 亲眼（最好亲手）经历一次"卡住→划词→读懂"的松弛感。
4. **"还有别人也这样"** —— 卡点数据让他知道这不是他一个人的问题，是普遍断点。

做到 1+2，评委愿意继续看；做到 3，评委信了；做到 4，评委记住了。

### 6.2 首屏即痛点（第一屏设计，逆向当前退化）

**当前问题**（此前诊断）：首页第一屏退化成"文章列表 + 功能说明"，把"三秒定生死"的洞察丢了。
**新首屏必须是"痛点现场"**，三种可选方案，按冲击力排序：

- **方案 A（最推荐）：一段真实知乎长文的开头，停在半路。**
  首屏直接是一篇真实长文（§1 官方 API 拉取）的前两段，第三段开头一个术语被虚线高亮，旁边浮一句：「**读到这的人，大多在这里停了。**」——不解释产品，先让评委感到"对，我就是"。
- **方案 B：一句扎心开场 + 一个真实数字。**
  大标题：「你收藏夹里，有没有一篇永远没读完的知乎长文？」副标题：「多半不是因为你懒，是读到一半卡在一个词上，然后就关了。」下面一个真实滚动数字：「本周 N 人在知伴读懂了他们本要放弃的文章」。
- **方案 C：左右对比。**
  左：「没有知伴的读法」——读到第三段，红框标"卡住"，箭头指向"关闭"。右：「有知伴的读法」——同位置划词，浮层弹出解释，箭头指向"读完」。

> 无论选哪个，**首屏禁止出现"功能说明 / 我们的特色 / 关于我们"**。那些放到第二屏以后。

### 6.3 真实场景复现（演示剧本 = 让评委亲历痛点→解决）

不要"讲解功能"，要"演一段真实阅读"。剧本写死在 Demo 里，评委跟着走：

| 步骤 | 评委看到/做到 | 第一感觉来源 |
|---|---|---|
| 1 | 打开一篇**真实知乎长文**（官方 API），不是范文 | "哦，是真的知乎" |
| 2 | 滚到中段，一个术语旁淡入提示：「这里，73% 的读者停过」 | "这说的就是我" |
| 3 | 评委**亲手**双击/划选那个词 | "我来试试" |
| 4 | 浮层弹出解释，**带本段语境 + 前置概念链**（如"损失函数→梯度下降"） | "原来如此"（松弛感） |
| 5 | 浮层下方一行：「和您一样卡在这的人，还卡在：导数、偏导数」 | "不是我一个人" |
| 6 | 翻到答主视角：这位答主看到「我的读者 1,283 人卡在链式法则」→ 补了一句前置说明 | "作者真的能用这个改进" |
| 7 | 回到读者视角，那段顶部多了一行「作者补充：建议先读梯度下降」 | "闭环了，真有用" |

> 关键：**第 3 步必须让评委自己动手**，动手=相信。纯播放视频达不到这个效果，所以演示要"真人走查 + 视频辅助"，不能只放录屏。

### 6.4 数据真实可信（让"别人也卡住"不是编的）

"1,283 人卡住"这种数字，编出来评委一眼假。**两条路，按可信度排序：**

- **路线 1（大胆规划，推荐）：让数据真的活起来。**
  把 §2.2 的"加分动作"升级为**核心**：SCF 加最小 `/stuck` 上报 + 聚合（`scf/index.js` 现有路由里确实没有 `/stuck`，`store.js` 当前只写 localStorage）。Demo 跑在公网，评委、观众、队友划词卡点都真实汇总。
  - 效果：演示当天数字是从真实流量里长出来的，"1,283"是现场累计，不是写死的 seed。
  - 即使没时间做社区版，**至少做"本场演示会话内聚合"**：同一台/同一链接的多设备、多次访问共享一个内存计数，评委当场划一次，数字 +1，下一个评委看到的是 +1 后的。
- **路线 2（诚实兜底）：若仍是 seed，必须这样呈现。**
  界面已标"演示环境数据"，但**不能只标一行小字**。要把它讲成设计原则：「我们刻意先做了单机隐私版——你的卡点只存在你浏览器里，这是隐私优先。社区汇总，是我们下一步。」把"没做后端"讲成"先保护你"，评委反而加分。

> **绝对禁止**：写死一个精确到个位的数字却不标注。那是演示现场最容易被问死的雷。

### 6.5 评委代入式交互（让他当读者，不当观众）

- **角色切换要丝滑**：Demo 顶部一个"我是读者 / 我是答主"开关，评委一秒切换，亲历两端。当前 `creator.js`（答主视角）和 `article.js`（读者视角）已分模块，只需补一个统一入口。
- **"我的痕迹"**：读者侧加"我的学习足迹"——读过几篇、卡过几个词、掌握了几个概念。哪怕只是本地 localStorage（`store.js` 已有基础），也能让人感觉"这是**我的**工具，不是展览品"。
- **临场社交感**（大胆项）：轻量轮询"当前 N 人在读""刚刚有人卡在 X"——哪怕数字半真半演，也能瞬间把"死"的 Demo 变成"活"的产品。

### 6.6 一句话击中（开场白 / 视频第一句 / 文档第一段统一用这版）

> **「你有没有一篇收藏了、却永远没读完的知乎长文？多半不是因为你懒——是读到一半卡在一个词上，然后就关了。知伴做的，就是让本来会放弃的人，读完。」**

这句同时命中：真实问题（放弃阅读）、不是痒点（懒 vs 卡）、知识炼金场的"吸收"断点。所有对外文案统一用它，不要各写各的。

### 6.7 阶段蓝图（不分时间，按"第一感觉成立度"递进）

> 不再排 Day1/2/3。按"做到哪一层，评委的第一感觉就稳了"来排。时间与人力只是资源，不是优先级。

- **P0 · 工程基线（已完成）**：测试全绿、部署与本地一致、前期 bug 类问题 7/8 已修。
- **P1 · 真实内容底座（§1）**：官方知识内容 API 接入 + 降级。→ 评委感到"这是真知乎"。
- **P2 · 首屏痛点重构（§6.2）**：第一屏从文章列表改成痛点现场。→ 评委感到"这说的就是我"。
- **P3 · 活的数据（§6.4 路线1）**：SCF `/stuck` 聚合，卡点真实回流。→ 评委感到"不是我一个人"。
- **P4 · 读者完整体验旅程 + 我的痕迹（§6.3 / §6.5）**：划词→读懂→足迹，亲手走通。→ 评委感到"它真解决了"。
- **P5 · 读者↔答主闭环（§6.3 步骤6-7）**：卡点回流答主 → 答主补说明 → 读者再读见到。→ 评委感到"这是社区生态的一环"。
- **P6 · 临场社交感（§6.5）**：在线人数、刚有人卡点。→ 评委感到"这东西现在就有人在用"。

> **最低交付线（保住第一感觉的底线）**：P1 + P2 + P4 剧本走通。P3/P5/P6 是让感觉从"信了"升到"记住"的递进，能做尽做，不做也不崩。

### 6.8 评审演示剧本（具体走查，串起上面所有层）

1. 开场白念 §6.6 那句话（10 秒，评委点头）。
2. 不切功能页，直接打开首屏那篇真实长文（方案 A），停在半路术语（§6.2）。
3. 把鼠标交给评委，让他划那个词（§6.3 步骤 3-4）——等他露出"哦"的表情。
4. 指给他看浮层下方的"和你一样卡住的人"（§6.4）——"你看，不是你一个人"。
5. 切"我是答主"，展示卡点汇总 + 补一句说明（§6.3 步骤 6）。
6. 切回读者，那一段顶部出现了"作者补充"（§6.3 步骤 7）——"闭环了"。
7. 收尾回到 §2.1 那张对比图：知乎知道赞/收藏，不知道"卡在哪"；知伴补的就是这条。

> 全程不出现"我们的特色""技术架构"这类词。让体验自己说话。架构图留给文档，不在演示里讲。
