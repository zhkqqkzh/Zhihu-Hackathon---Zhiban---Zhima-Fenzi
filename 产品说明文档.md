# 知伴 · 产品说明文档

> 黑客松提交材料 · 赛道：**📚 知识炼金场**
> 形态：Chrome 扩展（MV3）为主体，静态 Demo 站与云函数后端为辅
> 仓库：<https://gitee.com/alix-omix/zhihu-hackathon>

---

## 1. 一句话

**你有没有一篇收藏了、却永远没读完的知乎长文？多半不是因为你懒——是读到一半卡在一个词上，然后就关了。知伴做的，就是让本来会放弃的人，读完。**

读知乎遇到不懂的概念，划一下，知伴当场在这篇回答的语境里把它讲明白；你卡住的地方会匿名聚合，让下一个读到这里的读者少卡一次，也让答主知道该把什么讲在前面。

## 2. 解决的真实问题

知乎不缺优质长文，缺的是「读完」。深度学习、量子力学、医学——大量读者读到第三段就因为一个概念放弃了，这些知识「被写出来却没被吸收」。这正是知识炼金场「**吸收**」环节的断点：

- 不是「查词慢」（痒点，没人为省 3 秒装插件）；
- 是「**本来想读，卡在概念上，关了**」——每个人都经历过，却从没人量化。

**知乎知道一篇回答有多少赞、收藏、评论；不知道读者读到第几段、卡在哪个词。** 知伴补的正是这条空白数据。

## 3. 产品形态与功能

### 3.1 Chrome 扩展（主形态，真实场景）

注入 `zhihu.com/question/*` 与 `zhuanlan.zhihu.com/p/*`，读者侧完整闭环：

| 功能 | 说明 |
|---|---|
| 划词即问 | 选中概念弹「问知伴」，**流式**返回三层解释：一句话定义 + 它在本段的作用 + 前置概念链（如「损失函数 → 梯度下降」） |
| 全文预扫描 | 自动抽取本篇 5–15 个专业概念并高亮，划之前就知道哪里会卡 |
| 卡了一下 | 解释浮层一键上报卡点；本篇卡点 TOP3 可点回原文段落高亮 |
| 答主视角 | 侧栏 tab：当前回答的读者卡点报告（人次/占比/段落/来源）+ 「在开头补一句前置说明」的具体建议 |
| 看山提问 | 基于问过概念生成自测题，答完即评 |
| 每周复盘 | 零操作推到页面入口：本周问过多少、掌握率、最该补的 3 个概念 |
| 个人中心 | 独立页：学习足迹（回原文）、思维导图（依赖与缺口）、知识卡片、学习方向建议、知乎收藏夹体检 |

### 3.2 卡点飞轮（社区闭环）

```
读者划词卡了一下 → 匿名上报「概念名+段号」→ SCF /stuck 聚合
  → 侧栏「本篇卡点」：别的读者实时看到「N 人也卡在这」
  → 答主视角：作者看到读者卡在哪 → 补一句前置说明 → 下一任读者不再卡
```

数据三源在界面**如实标注**：`演示环境数据` / `你的上报` / `社区上报`，绝不把预置数据冒充真实统计。

## 4. 知乎开放能力的使用与取舍

| 能力 | 用没用 | 为什么 |
|---|---|---|
| 知乎搜索 API | ✅ 用 | 解释浮层「前置知识」一键跳知乎站内搜索，服务端代理（secret 不进前端） |
| 知乎 Web API（个人中心） | ✅ 用 | background 白名单只放行 `/api/v4/me`、`collections`、`collection items` 三个读接口，复用浏览器登录态做收藏夹体检 |
| 官方直答 | ❌ 不用 | 额度仅 100 次/日，撑不住高频划词；且我们需要「概念+本段语境」的精准流式解释，自建 GLM 可控、可流式、可带前置概念链 |
| 知识内容 API | ⏳ 路线图 | Demo 站文章源切换真实知乎正文的方案已设计（含降级兜底），本次未实施 |

主动讲清「为什么用 A 不用 B」：体现对官方能力的真实理解，而非为用而用。

## 5. 技术架构

```
知乎页面 ──content script──► background(SCF 转发/SSE 透传/知乎API白名单)
   │ 共享模块零改动复用              │
   │（core/* app/*）                ▼
   └── 侧栏/浮层/预扫描/复盘   腾讯云 SCF：/ask /prescan /search /quiz
        数据全在 chrome.storage.local   /collections /stuck /ping
                                       │
                                       ▼
                              智谱 GLM（glm-4.7-flashx，JSON 模式）
```

- **MV3**：密钥与跨域请求全在 background，页面 CSP 不感知；SSE 经端口逐 chunk 回推，逐字到达浮层。
- **质量保障**：非概念三层拦截（确定性短路 + 判定式提示词，划「的」不再浪费模型调用）；预扫描三层清洗，幽灵标记率实测 0.0%。
- **SCF 冷启动**：页面加载即 `warmup()` 预热，演示前建议先开一次页面。

## 6. 数据与隐私（设计原则，不是妥协）

- 正文、阅读记录、学习数据**全部存在本机** `chrome.storage.local`，侧栏常驻声明 + 一键清空；
- 唯一离开本机的是卡点：**只上报「概念名 + 段号」匿名计数**，服务端默认内存聚合、配 `STUCK_REDIS_URL` 即 Redis 持久化（冷启动 / 多实例共享、跨设备互见）；
- 先做单机隐私版把体验跑通保护读者数据，社区汇总以「读者同意共享」为下一步——这是路线图，不是没做完。

## 7. 运行方式

```bash
git clone <仓库> && npm install
npm run build:ext        # 打包到 extension/dist（MV3 禁止远程代码，全部本地打包）
# chrome://extensions → 开发者模式 → 加载已解压的扩展程序 → 选 extension/
```

打开任意知乎回答页即可使用；线上解释走知伴 SCF，无需任何配置。

## 8. 质量基线（2026-09-13 实测）

| 层 | 结果 |
|---|---|
| 语法+模块检查 | 63 文件 0 失败 |
| 核心算法单测 | 140 项 0 失败 |
| 学习中心 | 23 项 0 失败 |
| 后端接口 | 28 项 0 失败 |
| 端到端（Edge 无头 + CDP 真实划选） | 56 项 0 失败 |

线上 SCF：`/ping` 200；`/ask` 流式首字节 ~0ms、总耗时 610–642ms（热实例）；`/stuck` 上报→聚合回读全通。

## 9. 已知限制与路线图

- **移动端不做**：形态天花板，MV3 扩展只跑桌面 Chrome/Edge，如实声明；
- 答主回流闭环（补前置说明 → 读者侧可见「作者补充」）下一步深化；
- 卡点社区汇总下一步加「读者授权共享」开关；
- 演示环境的预置卡点数据已全程标注来源，正式环境数据全部来自真实上报。
