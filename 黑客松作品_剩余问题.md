# 黑客松作品 · 剩余问题（产品 / 演示说服力层）

> 前置：工程正确性 8 条已在 `问题清单.md` + `项目复查报告_2026-09-12.md` 覆盖，其中 7 条已修、1 条（zhihu.toply.top 坏链接）属部署域名范畴不在此列。
> 本文件只谈**之前没覆盖的、影响评审说服力的深层问题**。所有结论来自源码与线上实测，非推测。

---

## 问题 1 ｜【最致命】卡点飞轮是"叙事"不是"闭环"

**现象**：首页三秒洞察「8,432 赞同 / 第 2 段 1,283 人卡住 / 链式法则」、答主视角页「你的读者卡在这三个地方」，数字都来自 `STUCK_SEED`（`demo/js/core/stuck.js:19`）——演示预置数据。

**证据**：
- `demo/js/app/store.js:1` 注释写明：**"所有记录仅存本浏览器，不上传服务器"**
- `addStuckMark`（`store.js:178`）只写 `localStorage`，无后端调用
- `scf/index.js` 路由只有 `/ask /prescan /search /quiz /collections`，**没有 `/stuck`**，即后端根本不收卡点
- 用户自己卡住时上报的 marks 只存在自己浏览器，不会汇总给任何人

**为什么重要**：改造方案 §三 的核心卖点就是"把困惑从私有消耗品变成社区公共品 / 你划一下，让下一个读到的人少卡一次"。这正是和"社区生态契合""对社区用户真实价值"两条评审标准直接挂钩的部分。**但 Demo 里这个飞轮一次都没真转过**——所有"社区数据"都是预置的。评委一深挖"这数据哪来的"，答"演示预置的"，整个社区故事当场塌房。

**诚实的一面（要讲清的）**：团队确实标注了 `数据来源：演示环境数据`（`home.js:86`、`stuck.js:8`），没有伪造。这是个**隐私优先的刻意取舍**（数据不上传），不是造假。但"取舍"和"故事闭环"是两件事。

**建议（二选一，48 小时内能动的）**：
- A（推荐）：SCF 加一个最小 `/stuck` 路由（上报 + 按 concept 聚合读取），Demo 站内部署后，现场多台设备真能互相看到卡点，飞轮就转了。改动量：1 个后端函数 + `api.js` 加 `reportStuck` + `store.js` 的 `addStuckMark` 调一次后端。
- B：若不做后端，演示话术必须明确"这是单机隐私优先版，社区汇总是下一步"——把取舍讲成特性，而不是让评委自己发现。

---

## 问题 2 ｜ 演示数据标注不全，"赞数"像真统计

**现象**：首页洞察第一句「这篇回答有 **8,432** 人赞同」没有"演示"标注，而卡点那句有「数据来源：演示环境数据」。

**证据**：
- `demo/js/data/articles.js:11` `voteupCount: 8432` —— 写死
- `home.js:83` 渲染：`这篇回答有 <b>${formatCount(insight.voteupCount)}</b> 人赞同。` —— **无演示标注**
- 对比 `home.js:86` 卡点行明确带 `数据来源：${stuckSourceLabel(insight)}`

**为什么重要**：卡点部分诚实标了"演示环境数据"，赞数却没标。评委看到精确的"8,432 赞同"会默认这是真实知乎数据，一旦意识到是写死的，对全站数据的信任度会连带崩。

**建议**：要么给赞数也加「· 演示数据」小字标注，要么干脆用更明显的"示例回答"措辞（文章本身就是 Demo 内容，统一标"示例"最稳）。

---

## 问题 3 ｜ 核心场景没在真实知乎长文上跑过

**现象**：Demo 站点用的是内置 3 篇理想化 ML 文章（`data/articles.js`），不是真实知乎页面。

**证据**：`home.js:19` `const focus = ARTICLES[0];` 直接取内置数组；正文是手写的干净文本。

**为什么重要**："划词解释知乎长文"是项目的根。但演示时评委看到的是**完美排版的文本**，而真实知乎长文有：超长段落、图片/公式/引用块、乱 DOM 结构、预扫描词表在脏数据上的幽灵标记。插件版（`extension/`）才能在真实知乎跑，但**插件安装/落地链路在 Demo 站点体现不了**——评委手上是网页，不是装了插件浏览器。

**建议**：演示脚本里必须有一段"在真实知乎页面用插件划词"的录屏/截图，把根场景补上；否则 Demo 站给人的印象是"只能读自己写的范文"。

---

## 问题 4 ｜ 交互依赖型模块，评委全新浏览器打开时是空壳

**现象**：看山提问、每周复盘、掌握率、Learning Hub 都依赖用户**真的在 Demo 里卡住/展开概念/读到结尾**才会产生数据。

**证据**：
- `quiz.js:38` 的 `pool` 从 `store.listConcepts()` 取，全新浏览器为空 → 渲染"看山翻了翻笔记本，这篇还没有能问你的问题"（空引导）
- `review.js`、`difficulty.js` 同理依赖本地记录

**为什么重要**：评委用全新浏览器打开 Demo，这些"最有记忆点"的模块全是空状态/引导语，说服力弱。除非演示前提前点"载入示例数据"（`sidebar.js:254`，走 `sample.js`）。

**建议**：演示 Checklist 里加一条"开场前先载入示例数据，并把某篇读到结尾触发看山提问"，让交互模块在评委面前是'有内容'的状态，而不是空壳。

---

## 一句话总结

工程层已经健康（测试全绿、线上部署一致、前期 bug 已修）。**剩下的问题是"飞轮没闭环 + 演示数据信任 + 真实场景缺位"**——这些都是"让评委相信你真解决了问题"的层，不是"代码跑不跑"的层。其中 **问题 1 是唯一值得投入开发的**，其余三个靠演示脚本和话术就能化解。
