# 知伴 · 方向修改计划书（供代码执行用）

> 本计划书**只定义"改什么方向、改成什么样"**，不含具体代码实现。
> 执行 AI 应据此在现有仓库 `d:/project/知乎` 内改动，并遵守仓库既有约定（见第五节）。
> 目标：把"学习管理系统"重构为"阅读时的即时参谋"，让价值被动沉淀、零操作融入生活，且核心能力不依赖任何知乎接口也能 100% 工作。

---

## 一、背景与目标

**当前问题（已核实）**
- 功能堆砌：12 个功能并列，主路径不清。
- 入口错位：最强能力（收藏体检、看山提问真实效果）锁在桌面 Chrome 扩展 + 知乎登录态，评委/普通用户场景看不到。
- 头牌闭环为假："读者变作者"的发布 = 复制剪贴板 → 手动发（模拟）。
- 对非官方知乎 API 硬依赖：`/search`、`/collections` 被写进核心体验，又脆又有 ToS 风险。
- 场景矛盾：知乎阅读主场景在手机，最优能力在桌面扩展，且 README 仅一句话带过移动端。

**目标**
1. 一个瞬间价值（划词→解释）做到极致。
2. 沉淀被动化：用户不主动打开任何"中心/报告"，价值主动来找用户。
3. 核心零依赖：选中即问不依赖知乎接口；知乎能力全部可降级。
4. 闭环真实：去掉假发布，换成真实可用的"存笔记"。
5. 诚实定位：明确目标用户与移动端边界。

---

## 二、核心方向原则（北极星）

1. **一个瞬间价值做到极致**：划词 → 解释，<1s、流式、可缓存。
2. **沉淀被动化**：用户不主动打开任何"中心/报告"，价值主动来找用户。
3. **核心零依赖**：选中即问不依赖知乎接口；知乎能力全部可降级。
4. **闭环真实**：去掉假发布，换成真实可用的"存笔记"。
5. **诚实定位场景**：明确目标用户与移动端边界，不假装全覆盖。

---

## 三、分模块修改方向

> 每项格式：现状 / 改为 / 验收。文件指针仅用于定位，不写代码。

### 1. 产品主张与定位
- **现状**：slogan「读者变作者 / 知识炼金场」，文档叙事偏"管理学习"。
- **改为**：主张收敛为 *「读不懂，划一下，当场讲明白；你忘了它替你记」*。README 开篇、功能总览、结尾语全部按此重写；「知识炼金场」仅作为赛道标签保留，不作为产品主叙事。
- **验收**：README 无"学习中心/管理"主导叙事；主张一句话可概括。

### 2. 功能优先级重排（做减法）
- **现状**：12 功能并列。
- **改为**：
  - **P0 必做且极致**：选中即问（`demo/js/app/selection.js` / `popup.js` / `api.js` + SCF `/ask`）。
  - **P1 顺手**：全文概念标记（`demo/js/core/highlight.js` + prescan）。
  - **P2 降级/后续/仅留入口**：短尾巴、学习中心、导读、SRS、策展、收藏体检、个人中心——从 MVP 主路径移除或仅保留路由入口与占位，不再作为演示重点。
- **验收**：Demo 站首屏只突出 P0/P1；P2 不在主演示流中强制出现。

### 3. 沉淀从主动变被动（关键）
- **现状**：用户需主动打开「学习中心 / 冰屋」看图谱与诊断。
- **改为**：新增「每周复盘」被动推送——基于本地已存概念/文章数据，定时（或首次打开时若距上次 ≥7 天）生成一份极简复盘卡片：本周卡住概念数、掌握率、最该补的 3 个概念。推送到常驻入口 / 侧栏角标，**零用户操作**。原「学习中心」图谱/诊断可保留为复盘卡片的展开详情，但入口改为被推送触发，而非用户主动找。
- **验收**：用户不打开任何中心页面，也能在 7 天后看到一份自动出现的复盘；零配置、纯前端（不调模型或仅在生成时调一次）。

### 4. 假发布 → 真笔记
- **现状**：导读"发布"= 复制剪贴板 → 手动发（模拟）。
- **改为**：导读 / 单概念均可「存为我的笔记卡片」，本地存储（新增键 `zb:note:<id>`），支持一键导出 `.md` 文件下载。笔记卡片含：概念 / 定义 / 本篇语境 / 原文引用 / 来源链接 / 时间戳。删除"复制去知乎发布"的主路径，仅保留"导出分享"作为可选。
- **验收**：用户可无接口依赖地生成笔记并下载 `.md`；无"模拟发布"误导文案。

### 5. 解耦知乎非官方 API
- **现状**：`/search`、`/collections` 依赖 `ZHIHU_ACCESS_SECRET` 与读登录态；部分被写成核心体验。
- **改为**：选中即问与全文标记**完全不依赖**知乎接口（仅需页面文本 + LLM）。`/search`（前置知识跳转）、`/collections`（收藏体检）改为纯可降级加分项：未配置或失败则静默降级，不影响 P0/P1。README 明确"知乎接口为可选增强，非核心依赖"。
- **验收**：在完全没有 `ZHIHU_ACCESS_SECRET`、且浏览器无扩展注入的情况下，P0/P1 全功能可用。

### 6. 移动端与场景诚实化
- **现状**：README 仅一句话带过"移动端划选不可行"。
- **改为**：README 明确目标用户 = 桌面端深度阅读者（Chrome 扩展形态），并说明移动端为已知边界；若后续做移动，单独规划（小程序/公众号），不在本仓库主路径。移除"覆盖所有人"的隐含叙事。
- **验收**：README 有专门小节说明目标用户与移动端边界，无矛盾表述。

### 7. 赛道契合保留轻量创作出口
- **现状**：用假"发回答"硬凑"炼金/创作"。
- **改为**：真实创作出口 = 笔记卡片可一键导出 / 分享（见第 4 点）。既是真实笔记动作，又给赛道留"炼金"口子。不虚构发布流程。
- **验收**：创作出口真实可用且零接口依赖，不与"模拟发布"并存。

---

## 四、明确不做什么（范围边界）

- 不新增"学习管理系统"类重型功能。
- 不把知乎非官方 API 作为核心依赖加固。
- 不实现真实的知乎回答发布（违反平台规则且无接口）。
- 不在本阶段做移动端工程。

---

## 五、给执行 AI 的约束（必须遵守仓库既有约定）

- 遵守 README「三条铁律」：
  1. 存储全异步（经 `demo/js/core/storage.js` 接口，新环境在适配层 `setStorageAdapter()`）；
  2. DOM 选择器可配置（`demo/js/core/selectors.js`，禁止硬编码）；
  3. 环境差异只在适配层（core 内不得出现 `localStorage` / `chrome.*` / `fetch` 直连业务域名）。
- 纯算法放 `demo/js/core/`（纯函数、不碰 DOM），并补 `test:core` 或 `test:hub` 断言；
- 新后端能力：SCF `scf/index.js` 加路由（Node 12 兼容语法，不用 `?.`/`??`）+ `demo/js/app/api.js` 映射 + 补 `test:api`；
- 前端挂 `runtime.on(...)` 事件、数据经 `store.js` 读写；新路由在 `router.js` 注册、`main.js` 的 `dispatch` 分发。
- 改动后保证 `npm run check` 与 `test:core` / `test:hub` / `test:api` 全过；e2e（`scripts/test-e2e.mjs`）逻辑同步更新（若涉及 P0/P1 路径）。
- 不引入新的**运行时**三方依赖（保持"运行时零三方依赖"）；如需新包仅在 `devDependencies`。

---

## 六、验收总标准

1. 核心主张一句话可概括，README 叙事一致。
2. **P0 选中即问在无知乎接口、无扩展情况下 100% 可用**。
3. 出现「每周复盘」被动推送，零用户操作触发。
4. 笔记可导出 `.md`，闭环真实。
5. 所有原 P2 重型功能不阻断主路径、不误导。
6. 全量自动化测试（core/hub/api）通过。

---

*本计划书为方向层，代码执行请严格对照上述「改为 / 验收」与第五节约束。*
