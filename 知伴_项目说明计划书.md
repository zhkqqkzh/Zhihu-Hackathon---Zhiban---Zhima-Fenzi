# 知伴 · 和看山一起读 —— 项目说明计划书

> 知乎黑客松 2026 校园新锐季 · 知识炼金场赛道
>
> 团队：迟遇
> 形态：Chrome 扩展（MV3）+ 静态 Demo 站 + 腾讯云 SCF 云函数后端

---

## 目录

1. [项目概述](#1-项目概述)
2. [适配知乎场景的价值分析](#2-适配知乎场景的价值分析)
3. [创作思路与产品理念](#3-创作思路与产品理念)
4. [技术方案详解](#4-技术方案详解)
5. [核心创新点](#5-核心创新点)
6. [知乎开放能力的使用与取舍](#6-知乎开放能力的使用与取舍)
7. [质量基线](#7-质量基线)
8. [运行方式与部署](#8-运行方式与部署)
9. [已知限制与路线图](#9-已知限制与路线图)

---

## 1. 项目概述

### 一句话

读知乎遇到不懂的概念，**划一下**，当场在这篇回答的语境里讲明白；你卡住的地方，聚合成答主看得见的「读者卡点报告」，让下一个读到这里的人不再卡住。

### 解决的问题

知乎不缺优质长文，缺的是「读完」。深度学习、量子力学、医学、法学——大量读者读到第三段就因为一个陌生概念放弃了整篇回答。这些知识「被写出来却没被吸收」。

**核心洞察**：不是「查词慢」（痒点，没人为省 3 秒装插件）；是「本来想读，卡在概念上，顺手就关了」——每个人都经历过，却从没人量化过。

**空白数据**：知乎知道一篇回答有多少赞、收藏、评论；**不知道读者读到第几段、卡在哪个词**。知伴补的正是这条空白数据，并以此构建社区闭环。

### 交付形态

| 形态 | 用途 | 链接 |
|---|---|---|
| Chrome 扩展（MV3） | 主产品，注入知乎回答页/专栏页 | 本地打包加载 |
| 静态官网 | 项目介绍落地页 | https://zh.toply.top/ |
| 线上 Demo | 个人中心原型预览 | https://z.toply.top/zhiban-demo/ |
| 腾讯云 SCF | AI 推理与卡点聚合后端 | 云函数部署 |

---

## 2. 适配知乎场景的价值分析

### 2.1 为什么是知乎？

知乎的内容生态有两个鲜明特征：

**特征一：高信息密度的深度长文**

知乎不同于微博、小红书等碎片化平台，其核心内容是带有专业门槛的长文回答。一篇关于「Transformer 架构」或「高血压用药原则」的回答，可能在第三段就出现读者不懂的概念。这种「高密度知识」场景天然需要阅读辅助工具。

**特征二：答主与读者的单向关系**

目前知乎的互动模型是：答主写 → 读者读 → 读者点赞/收藏/评论。但答主**不知道读者在哪里放弃**，读者**不知道自己卡住的地方别人也卡过**。这是一条断裂的信息流。

### 2.2 知识炼金场赛道的适配性

赛道要求「把知识从原始材料中提炼、加工、沉淀」。知伴的三层价值与此高度吻合：

| 炼金环节 | 知伴的功能映射 |
|---|---|
| **提炼**（从文本中识别知识） | 全文预扫描自动抽取专业概念并高亮 |
| **加工**（降低理解门槛） | 划词即问，在上下文语境中做出三层解释 |
| **沉淀**（让知识可复用） | 笔记卡片 + Markdown 导出 + 间隔复习 |
| **回流**（反哺社区） | 卡点飞轮：读者卡点 → 答主补充 → 社区收益 |

### 2.3 社区场景价值

**对读者**：不再是孤独地卡住。知伴在你读不懂时当场讲明白，在你忘的时候提醒你复习，在你卡住的时候告诉你「这里 N 个人也卡过」。

**对答主**：第一次获得可量化的读者理解反馈——读者在哪个概念、哪个段落放弃。知伴还会给出「在开头补一句前置说明」的具体建议，让答主用最低成本提升回答的可读性。

**对社区**：卡点数据匿名聚合后，形成「这篇回答的阅读理解热力图」。新读者读之前就知道哪里可能卡，答主优化后更多人能读完——优质内容的价值被真正释放，而非沉没在收藏夹里。

---

## 3. 创作思路与产品理念

### 3.1 从自身经历出发

团队每个人都在知乎上有过这样的体验：打开一篇深度长文，读到一半遇到一个陌生概念，想查又懒得切出去搜，最终关掉了页面。第二天又在收藏夹里看到同一篇，点开，又从第一段开始读——循环往复。

**「要是有人能在我卡住的时候当场讲明白就好了。」** 这就是知伴最原始的动机。

### 3.2 设计原则

**原则一：不打扰，不打断**

知伴不会在页面上添加任何永久性 UI 元素。它只在两种情况下出现：你选中了一个词（划词浮层），或者你主动打开侧栏。预扫描的高亮是淡色波浪线，不影响正常阅读。

**原则二：数据隐私是默认项，不是可选项**

所有阅读记录、学习数据、笔记卡片**全部存在本机** `chrome.storage.local`。唯一离开本机的数据是卡点上报——且只上报「概念名 + 段号」的匿名计数。界面全程标注数据来源（演示数据 / 你的上报 / 社区上报），绝不把预置数据冒充真实统计。

**原则三：不做模拟的发布，只做真实的笔记**

知乎官方接口只读，知伴不做「一键发布到知乎」。笔记卡片导出为 `.md` 文件，这是真实的笔记，不是模拟的社交行为。

### 3.3 产品名由来

「知伴」= 知乎 × 陪伴。看山是知乎的 IP 形象，「和看山一起读」传递的是一种轻松、有人陪伴的阅读体验——你不是一个人在硬啃。

---

## 4. 技术方案详解

### 4.1 整体架构

```
┌──────────────────┐    代理转发     ┌──────────────────┐    HTTPS    ┌──────────────┐
│  浏览器端          │ ─────────────→ │  腾讯云 SCF       │ ─────────→ │ 智谱 GLM     │
│  ┌─────────────┐  │                │  Web 函数          │            │ glm-4.7-flashx│
│  │ content.js  │  │ ←── SSE 流 ────│  /ask（流式解释）   │ ←───────── │              │
│  │（知乎页面注入）│  │                │  /prescan（预扫描）  │            └──────────────┘
│  │ background  │  │                │  /search（知识搜索） │ ─────────→ ┌──────────────┐
│  │（中转/白名单）│  │                │  /quiz（看山提问）   │            │ 知乎开放平台  │
│  │ profile-page│  │                │  /stuck（卡点聚合）  │            └──────────────┘
│  └─────────────┘  │                │  /collections（收藏）│
│                   │                └──────────────────┘
│  用户数据全部       │                      ↑ 冷启动预热
│  chrome.storage    │                （页面加载即 warmup()）
│  .local            │
└──────────────────┘
```

### 4.2 浏览器端（Chrome Extension MV3）

**为什么选 MV3？**
- 密钥与跨域请求全在 background service worker，页面 CSP 不感知
- SSE 经 runtime端口逐 chunk 回推，浮层逐字渲染
- `chrome.storage.local` 提供可靠的本地存储

**模块结构**：

```
extension/src/
├── content.js           # 知乎适配层：CSP 绕行、划词拦截、SPA 路由变化检测
├── background.js        # 请求中转 + 知乎 API 白名单转发 + 流式端口桥接
├── profile-page.js      # 个人中心独立页（复用 demo/js/app/ 与 demo/js/core/）
└── profile-zhihu.js     # 知乎账号信息 + 收藏夹体检
```

**共享模块**（与 Demo 站共用，零改动复用）：

```
demo/js/core/            # 环境无关的纯函数模块
├── match.js             # 概念匹配算法
├── textnodes.js         # 文本节点遍历
├── highlight.js         # 高亮渲染（CSS Custom Highlight API，零 DOM 变更）
├── context.js           # 上下文提取
├── graph.js             # 知识图谱构建
├── srs.js               # 间隔复习算法
├── difficulty.js        # 难度评估
├── stopwords.js         # 停用词过滤
├── note.js              # 笔记卡片
├── review.js            # 复习调度
├── stuck.js             # 卡点上报
├── storage.js           # 存储抽象层
├── quote.js             # 引用提取
└── selectors.js         # CSS 选择器
```

**关键实现细节**：

- **划词即问**：`selectionchange` 事件拦截 → 去抖 300ms → 文本清洗 → 非概念三层拦截 → 端口转发 background → SCF `/ask` → SSE 流式回推 → 浮层逐字渲染
- **全文预扫描**：页面 idle 时触发 → `textnodes.js` 遍历可见文本 → `match.js` 模糊匹配 → 去重 → CSS Custom Highlight 波浪线标注（零 DOM 变更）
- **SPA 路由检测**：知乎页面使用 PJAX 部分刷新，`MutationObserver` 监听 `title` 变化检测路由切换

### 4.3 云函数后端（腾讯云 SCF）

**为什么选 SCF？**
- 零运维，按调用计费，适合黑客松项目规模
- Web 函数模式天然支持 HTTP 触发 + SSE 流式响应
- 冷启动靠 `warmup()` 预热，演示前先开一次页面即可

**接口清单**：

| 接口 | 方法 | 说明 | 关键参数 |
|---|---|---|---|
| `/ping` | GET | 健康检查 | - |
| `/ask` | POST | 流式概念解释 | `concept`, `context`, `stream:true` |
| `/prescan` | POST | 全文概念抽取 | `text`, `title` |
| `/search` | GET | 知乎搜索代理 | `q`（中转，密钥不进前端） |
| `/quiz` | POST | 看山提问生成 | `concepts[]`, `difficulty` |
| `/collections` | GET | 收藏夹分析 | 需知乎 Cookie 转发 |
| `/stuck` | POST/GET | 卡点上报与聚合 | `concept`, `paragraph`, `articleKey` |

**LLM 集成**：

- 模型：智谱 `glm-4.7-flashx`
- 模式：JSON Mode，强制结构化输出
- 流式：`stream:true` → SCF 透传 SSE 增量 → 前端 `EventSource` + `reasoning_content` 处理
- 非概念三层拦截（前置短路，避免浪费模型调用）：
  1. **确定性短路**：长度 <2 字符 / 纯数字 / 纯标点 → 直接返回非概念
  2. **词表过滤**：匹配停用词表 → 返回非概念
  3. **判定式 prompt**：让模型自行判断选中文本是否为专业概念

**卡点聚合存储**：

- 默认：内存 `Map`（适合演示 / 单实例）
- 可选：`STUCK_REDIS_URL` 环境变量 → Redis 持久化（多实例共享 / 跨设备互见）
- Redis 不可用时自动降级回内存，不悬挂到函数超时

### 4.4 数据流与隐私设计

```
┌─────────────────────────────────────────────────┐
│ 浏览器端（chrome.storage.local）                   │
│                                                   │
│  阅读记录  │  学习数据  │  笔记卡片  │  复习记录     │
│  ──────────┼──────────┼───────────┼───────────── │
│  全部本地   │  全部本地  │  全部本地   │  全部本地    │
│  不上传     │  不上传    │  不上传     │  不上传     │
└─────────────────────────────────────────────────┘
                          │
           只有卡点数据离开本机
           只上报：概念名 + 段号（匿名）
                          │
                          ▼
┌─────────────────────────────────────────────────┐
│ 腾讯云 SCF（内存 / Redis）                         │
│                                                   │
│  卡点聚合：{ 文章ID: { 概念: { 段号: 计数 } } }    │
│  不存储用户身份、不关联知乎账号                     │
└─────────────────────────────────────────────────┘
```

---

## 5. 核心创新点

### 5.1 卡点飞轮（社区闭环）

这是知伴最独特、与其他阅读辅助工具最本质的区别：

```
读者划词卡了一下 → 匿名上报「概念名 + 段号」
  → 侧栏「本篇卡点 TOP3」：其它读者实时看到「N 人也卡在这」
  → 答主视角（#/creator）：作者看到读者卡在哪
  → 「在开头补一句前置说明」的具体建议
  → 答主采纳后，下一任读者不再卡
```

**这个闭环的价值**：
- 第一次把「读者卡住」这个零散体验量化为可聚合的数据
- 第一次让答主获得阅读理解的真实反馈
- 每一个读者的「卡了一下」，都在为下一个读者铺路

### 5.2 上下文感知的 AI 解释

不同于通用搜索或词典，知伴的解释是**语境敏感**的：

- **一句话定义**：不超过 40 字，让你秒懂
- **为什么在这篇里重要**：紧扣当前段落上下文，不泛泛而谈
- **前置概念链**：如「损失函数 → 梯度下降 → 反向传播」，告诉你读之前需要先知道什么

### 5.3 零 DOM 变更的高亮方案

使用 CSS Custom Highlight API 实现预扫描高亮，不向 DOM 树插入任何 `<mark>` 或 `<span>` 标签：
- 不影响页面布局和重排性能
- 不干扰知乎原有的 DOM 结构和事件监听
- 概念名逐字照抄原文做匹配，零改写，避免「幽灵标记」

### 5.4 非概念三层拦截

选中「的」「了」「然后」等无意义片段时，先经过三层拦截再决定是否调用模型：
1. 确定性短路（长度/纯数字/纯标点）
2. 停用词表过滤
3. 判定式 prompt

避免浪费模型调用，也不会编出一段假装有定义的解释。

---

## 6. 知乎开放能力的使用与取舍

本项目对知乎开放能力的态度：**用在对的地方，不用在不需要的地方。**

| 能力 | 使用情况 | 原因 |
|---|---|---|
| 知乎搜索 API | ✅ 使用 | 解释浮层「前置知识」一键跳转知乎站内搜索，服务端代理（secret 不进前端） |
| 知乎 Web API（个人中心） | ✅ 使用 | background 白名单只放行 `/api/v4/me`、`collections`、`collection items` 三个只读接口，复用浏览器登录态做收藏夹体检 |
| 官方直答 | ❌ 不使用 | 额度仅 100 次/日，撑不住高频划词；且知伴需要「概念 + 本段语境」的精准流式解释，自建 GLM 可控、可流式、可带前置概念链 |
| 知识内容 API | ⏳ 路线图 | Demo 站文章源切换真实知乎正文的方案已设计（含降级兜底），本次未实施 |

**重要原则**：知乎接口只是可选增强，不是核心依赖：
- `/search`、`/collections` 在未配置密钥、或无扩展注入、或调用失败时**一律静默降级**
- P0 选中即问与 P1 全文标记仅需页面文本 + LLM，完全不依赖知乎接口

---

## 7. 质量基线

> 2026-09-13 实测

| 测试层 | 范围 | 结果 |
|---|---|---|
| 语法+模块检查 | 67 文件 | 0 失败 |
| 核心算法单测 | match / textnodes / highlight / context / graph / srs / difficulty / stopwords / quote / selectors / storage / note / review / stuck | 162 项 0 失败 |
| 学习中心 | home / profile / hub / igloo / weekly | 23 项 0 失败 |
| 后端接口 | /ping /ask /prescan /search /quiz /collections /stuck | 28 项 0 失败 |
| 端到端测试 | Edge 无头浏览器 + CDP 协议真实划选操作 | 73 项 0 失败 |

**线上 SCF 实测**：
- `/ping`：200 OK
- `/ask` 流式：首字节 ~0ms（热实例），总耗时 610–642ms
- `/stuck` 上报 → 聚合回读：全通

---

## 8. 运行方式与部署

### 8.1 本地开发

```bash
git clone <仓库>
npm install              # 仅开发依赖（esbuild、express）

npm run dev              # 启动本地服务器 → http://localhost:8787

# 本地接真实模型（可选）
export LLM_API_KEY=xxx
export LLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4
export LLM_MODEL=glm-4.5-flash

# 插件构建
npm run build:ext        # 打包到 extension/dist
```

### 8.2 加载扩展

1. 打开 `chrome://extensions`
2. 开启「开发者模式」
3. 点击「加载已解压的扩展程序」
4. 选择 `extension/` 目录
5. 打开任意知乎回答页即可使用

### 8.3 生产部署

| 组件 | 部署方式 |
|---|---|
| Chrome 扩展 | `extension/dist/` 本地加载，不上架商店 |
| 官网 | 宝塔面板创建静态站点 → 上传 `site/index.html` |
| Demo 站 | 同静态站点部署，路由由前端 hash 处理 |
| SCF 云函数 | 腾讯云 SCF Web 函数，Node.js 自定义运行时，入口 `scf/index.js` |

### 8.4 关键环境变量

| 变量 | 用途 | 必填 |
|---|---|---|
| `LLM_API_KEY` | 智谱 GLM 密钥 | 是（生产） |
| `LLM_BASE_URL` | 模型 API 地址 | 否（有默认值） |
| `LLM_MODEL` | 模型名 | 否（默认 glm-4.7-flashx） |
| `ZHIHU_COOKIE` | 知乎 Cookie（用于搜索代理） | 否（限流降级） |
| `STUCK_REDIS_URL` | Redis 连接串（卡点持久化） | 否（默认内存） |

---

## 9. 已知限制与路线图

### 已知限制（如实声明）

- **移动端不做**：知乎的阅读主场景在手机，而移动端无法承载 MV3 扩展与划选交互，这是形态天花板，如实声明
- **知乎接口为可选增强**：`/search`、`/collections` 在无扩展注入或调用失败时静默降级，P0/P1 功能不受影响
- **收藏夹体检依赖插件环境**：需读知乎登录态，Demo 站（无扩展注入）只给出引导文案
- **卡点聚合默认内存**：未配 `STUCK_REDIS_URL` 时冷启动或多实例下会归零；界面始终如实标注数据来源

### 路线图

| 阶段 | 项目 | 优先级 |
|---|---|---|
| 近期 | 答主回流闭环深化（作者补充 → 读者可见） | P1 |
| 近期 | 卡点社区汇总加「读者授权共享」开关 | P1 |
| 中期 | 知识内容 API 接入（真实知乎正文源） | P2 |
| 中期 | 难度预告与短尾巴回访（零模型调用，纯前端） | P2 |
| 远期 | SCF 多 Region 部署 + Redis Cluster | P3 |

---

> 读不懂，划一下，当场讲明白；你忘了，它替你记着。
> 你卡住的地方，也让别人少卡一次。
>
> —— 迟遇 · 知乎黑客松 2026 校园新锐季 · 知识炼金场赛道

**仓库地址**：https://github.com/zhkqqkzh/Zhihu-Hackathon---Zhiban---Zhima-Fenzi

**线上入口**：
- 官网：https://zh.toply.top/
- Demo 个人中心：https://z.toply.top/zhiban-demo/
