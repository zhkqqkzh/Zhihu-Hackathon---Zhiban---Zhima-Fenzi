# 知伴 · 待修问题清单

> 本文**只记录问题，不含任何代码改动**。修复由他人执行。
> 方向层参考：同目录 `真实痛点改造方案.md`。
> 所有结论来自 2026-09-12 对线上环境的实际调用，不是代码推测。每条都给了复现方式与验收标准，可直接转成工单。

---

## 零、一句话概括

代码本身是健康的（测试全绿、CORS 正常、流式性能好、解释质量高）。
问题分两层：**技术层有 5 个确定的缺陷**，**产品层有 1 个方向性偏差**。

---

## 一、优先级总览

| 编号 | 问题 | 层级 | 严重度 | 修复成本 |
|---|---|---|---|---|
| P0-1 | 结构漂移会被判为失败，白丢一次正确解释 | 技术 | 高 | 极小 |
| P0-2 | 第三层「非概念拦截」是死代码，从未生效 | 技术 | 高 | 小 |
| P0-3 | 线上 Demo 前端地址无记录（提交合规风险） | 交付 | 高 | 待确认 |
| P1-1 | 预扫描幽灵标记率会偶发突破 5% 门槛 | 技术 | 中 | 小 |
| P1-2 | 预扫描词表混入全文高频通用词 | 技术 | 中 | 小 |
| P1-3 | 预扫描耗时 1.7–11.3 秒，缺少过程提示 | 技术 | 中 | 小 |
| P2-1 | 仓库无插件构建产物，评委无法直接加载 | 交付 | 低 | 极小 |
| P2-2 | 首屏没有洞察，三秒内看不出产品解决什么 | 产品 | 高 | 中 |

---

## 二、技术问题详单

### P0-1 结构漂移会被判为失败，白丢一次正确解释

**现象**
模型偶尔把整个结果多包一层。此时前端拿不到 `definition`，直接把一次成功的解释判为失败。

**实测证据**
`POST /ask` 传 `term="的"`，实际返回：
```json
{"answer":{"definition":"用在定语和名词之间，表示修饰关系。","context_why":"…","prerequisites":["修饰关系"]}}
```
而正常返回是平铺的：
```json
{"definition":"沿着负梯度方向更新参数，逐步减小损失。","context_why":"…","prerequisites":["损失函数"]}
```

**定位**
- `demo/js/app/api.js` 第 34–44 行 `normalizeAsk`：只读 `r.definition`，不认 `r.answer` 包装
- `demo/js/app/popup.js` 第 72–76 行 `isUsableExplain`：判定不可用
- `demo/js/app/popup.js` 第 116 行：回落到 `fallbackExplain`

**根因**
`scf/index.js` 第 145 行用 `response_format: {type:'json_object'}`，这只保证输出是合法 JSON，**不保证结构符合预期**。方案 §11.9 已预警过这个风险，实际确实发生了。

**影响**
不是渲染空白（前端有兜底），而是**用户看到「「X」的解释这次没拿到，可能是模型繁忙」——一次本来正确的解释被丢掉**。

**建议修法**
解包逻辑写成纯函数放 `demo/js/core/`（符合项目「纯算法放 core」的约定），`normalizeAsk` 调用它。同时补 `test:core` 断言。

**验收**
构造 `{"answer":{...}}` 与平铺两种输入，两者都能取出 `definition`；`npm run test:core` 通过。

---

### P0-2 第三层「非概念拦截」是死代码，从未生效

**现象**
代码注释声明三层拦截，但第三层从未触发过。用户选中停用词表之外的虚词或泛词，会被当成概念送去解释。

**实测证据**
`POST /ask` 传 `term="的"`，后端返回了完整词条解释，未做任何拦截。

**定位**
- `demo/js/app/popup.js` 第 3 行注释："三层——非空 → 停用词表 → **模型判定 is_concept**"
- `demo/js/app/api.js` 第 36 行：`is_concept: r.is_concept !== false` —— 前端在读
- `demo/js/app/popup.js` 第 74 行、第 215 行：前端在判
- `scf/index.js` 第 45–50 行 `buildPrompt` 与第 234–265 行 `/ask` 路由：**从不生成、也从不返回 `is_concept`**

**根因**
协议里预留了字段，后端始终没实现。前端整条链路是通的，只要后端开始返回这个字段就能立刻生效。

**影响**
第一层（`demo/js/core/stopwords.js`，约 60 个词）是精确匹配，覆盖有限。"这个问题""另一方面""综上所述"这类词会全部漏过，被解释成概念。

**建议修法**
`buildPrompt` 增加「若非概念则返回 `is_concept:false`」的判定要求，`/ask` 一并返回该字段。注意流式与非流式两条路径都用同一个 `buildPrompt`，改动天然一致。

**验收**
`POST /ask` 传一个明显非概念的长短语，返回 `is_concept:false`；前端浮层出现「不是概念」提示而非解释内容。

---

### P1-1 预扫描幽灵标记率会偶发突破 5% 门槛

**现象**
预扫描返回的概念词，偶尔有几个在正文里逐字匹配不到（方案 §10.5 称"幽灵标记"）。

**实测证据**（对 Demo 三篇真实文章跑线上 `/prescan`）

| 文章 | 概念数 | 幽灵 | 丢失率 | 门槛 |
|---|---|---|---|---|
| article-backprop | 13 | 0 | 0.0% | <5% |
| article-gradient-descent | 15 | 1 | **6.7%** | 超门槛 |
| article-derivative | 12 | 0 | 0.0% | — |

幽灵词实例：`局部最优`。正文里实际写的是"局部的浅坑""全局最优解"，模型做了改写。

**定位**
`scf/index.js` 第 53–58 行 `PRESCAN_PROMPT`（约束已写但模型未严格遵守）

**根因**
模型每次输出不同，属概率性问题。当前没有任何前端自检兜底，超门槛只能靠运气。

**影响**
幽灵词在正文里匹配不到，高亮不出现，词表与页面不一致。

**建议修法**
前端在把词表落库前先跑一次逐字匹配自检，**丢弃匹配不到的词**（成本为零，且能从根上消除幽灵标记）。方案 §10.5 本来就要求统计匹配率，只是目前只统计未处理。

**验收**
同一篇文章重复跑 10 次 `/prescan`，落库后的词表丢失率恒为 0%。

---

### P1-2 预扫描词表混入全文高频通用词

**现象**
词表里出现"人人皆知"的通用词，这些词在正文里高频出现，一旦全部高亮，整篇会布满波浪线。

**实测证据**
`article-gradient-descent` 返回的词表含：`函数`、`参数`、`损失`、`搜索问题`。
`article-backprop` 返回的词表含：`参数`、`搜索问题`。

**定位**
`scf/index.js` 第 53–58 行 `PRESCAN_PROMPT`——已写"不输出…人人皆知的通用词"，但模型未遵守。

**影响**
比幽灵标记更伤视觉：清空数据后打开一篇文章，正文里"参数"出现十余次，全部带波浪线，整页失去可读性。

**建议修法**
两层：提示词加明确负面清单 + 前端做**频次过滤**（在正文中出现次数超过阈值的词直接丢弃）。频次过滤是纯函数，放 `core` 并补断言。

**验收**
Demo 三篇文章的最终高亮词表里，不存在正文出现超过阈值的高频词。

---

### P1-3 预扫描耗时 1.7–11.3 秒，缺少过程提示

**现象**
全文概念标记是"问完第一个词后台顺手扫完"的体验承诺，但实际可能超过 10 秒。

**实测证据**
| 文章 | 耗时 |
|---|---|
| article-backprop | 3630ms |
| article-gradient-descent | 11313ms |
| article-derivative | 1723ms |

**定位**
`demo/js/app/prescan.js`；提示文案在 `demo/js/app/**` 内未检索到相关字符串

**影响**
11 秒无任何反馈，用户会认为"没反应"或"坏了"。

**建议修法**
确认或补一个「正在扫描全文概念…」的轻提示（不阻塞划词）。预扫描本身是懒触发、可降级的，不影响 P0。

**验收**
清除缓存后打开一篇长文，11 秒内界面上能看到明确的"扫描中"状态。

---

### P2-1 仓库无插件构建产物，评委无法直接加载

**现象**
`extension/manifest.json` 第 13 行指向 `dist/background.js`，但 `.gitignore` 忽略了 `extension/dist/`。

**影响**
评委克隆仓库后无法直接"加载已解压的扩展程序"，必须先 `npm install && npm run build:ext`。

**说明**
构建产物不入库是正确做法，**不建议改**。但必须在提交说明或 README 显著位置写清这三步，否则评审现场加载插件会失败。

**验收**
一个不了解项目的人，只看 README 就能成功加载插件。

---

## 三、产品层问题

### P2-2 首屏没有洞察，三秒内看不出产品解决什么

**现象**
`demo/js/app/home.js` 第 15–24 行：第一屏是标题 + 副标题 + 三篇文章卡片。
评审打开后三秒内看到的是"一个讲机器学习的文章列表"。

**与自身方案的冲突**
方案 §18.2 明确写着"三秒定生死"，要求第一屏直接展示一篇已生成的导读 + 三句话。实际实现里导读被降级为 P2，首屏让位给了文章列表——**做减法时减掉了钩子，留下了功能**。

**影响**
这是评审的第一印象，权重极高。

**方向与做法**
见 `真实痛点改造方案.md` 第四节。要点：首屏直接呈现"这篇回答被收藏了 X 次，但 N 成的人在第 3 段就卡住了"，把用户真实困境摆出来，而不是罗列功能入口。

---

## 四、已验证正常（不要动）

以下均为实测通过，修复时不要顺手改：

| 项 | 实测结果 |
|---|---|
| CORS | 预检 204、`Access-Control-Allow-Origin` 回显 Origin、`ACAH/ACAM` 正常 |
| 流式性能 | 首字节 **376–691ms**，总时长 699–1743ms，`content-type: text/event-stream` 正确 |
| 解释质量 | 连续 6 个 ML 概念全部成功，定义准确、语境紧扣片段、前置概念合理 |
| 知乎站内搜索 | **已配置 `ZHIHU_ACCESS_SECRET`，返回真实知乎结果**（前置知识跳转可正常演示） |
| 密钥安全 | 无硬编码密钥；`api key.md` 已被 `.gitignore` 排除，且 git 历史从未提交过 |
| 测试基线 | check 57 文件 0 失败 / core 63 通过 / hub 23 通过 / api 22 通过 |

补充说明：非流式 `/ask` 实测耗时 10–21 秒，但**线上前端走的是流式**（`api.js` 第 11 行 `STREAM_EXPLAIN = !IS_LOCAL`），不影响线上体验，本地 dev server 会遇到。冷启动首次非流式请求实测 26.8 秒——建议加一次页面级预热，成本极低，可消除风险。

---

## 五、待确认（需项目负责人回答）

1. **线上 Demo 前端的公网地址是什么？**
   README 第 254–255 行只写"COS 桶开静态网站，上传 `demo/`"，项目内没有任何部署地址记录。
   评审必交材料第 1 条是"可公开访问并能实际操作的线上 Demo"——**这是提交前最需要先确认的一件事**。
2. 预扫描的"扫描中"提示是否已实现（我在 app 层没检索到相关文案）？
3. SCF 函数的执行超时是否已确认为 60 秒？（首次冷启动非流式实测 26.8 秒，留有余量但不算宽裕）

---

## 六、修复顺序建议

1. **先确认 P0-3**（线上 Demo 地址）——这是提交的硬门槛，其他都排在它后面
2. **P0-1 + P0-2**——两个都是小改动，改完解释链路的可靠性上一个台阶
3. **P1-1 + P1-2**——同一处（词表后处理），可以一次改完，改完 P1 全文标记的质量才站得住
4. **P2-2**——首屏改版，影响评审第一印象，但改动独立，可并行
5. **P1-3 + P2-1**——收尾
