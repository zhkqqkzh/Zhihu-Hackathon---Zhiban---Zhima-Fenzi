# 知伴 · 真实痛点改造方案

> 面向 48 小时开发窗口（9/13 10:00 — 9/15 10:00）的可执行方案。
> 结论先行：**工程质量远高于产品洞察。问题不在做得不够多，在于优化错了分支。**

---

## 一、现状实测（不是推测）

线上环境 `https://1399201542-7y33vuteqi.ap-beijing.tencentscf.com` 实测结果：

| 实测项 | 结果 | 判断 |
|---|---|---|
| `GET /ping` | `{"ok":true}` | 后端在线 |
| `POST /ask` 梯度下降 + ML 语境 | 「沿着负梯度方向更新参数，逐步减小损失」，前置「损失函数」 | 准确、贴语境 |
| `POST /ask` 反向传播 | 「利用链式法则，将误差从输出层传回，指导参数调整的算法」，前置「链式法则」 | 准确 |
| `POST /ask` 虚词「的」 | 返回了完整解释，未拦截 | 后端缺兜底 |
| 首字延迟 | 首次 26.8s，随后 1.0s | 冷启动问题 |

核心链路真实可用。这不是一个"没做完"的项目，是一个"做完了但方向需要校准"的项目。

---

## 二、核心诊断

### 2.1 优化落在少数分支上

读者遇到陌生概念后分两条路：

- 少数：划词求助 → 得到解释 → 继续读（**产品全部优化在这里**）
- 多数：顿一下 → 囫囵跳过 → 关掉页面，再没打开

产品的整个价值挂在一个前提上——"用户会主动划选"。而这个前提恰好是不成立的那个。
把最难的一步（维持阅读意愿）留给了用户，只优化了最容易的一步（查询速度）。

### 2.2 一句话检验

**如果一个用户不知道知伴存在，他的损失是什么？**

答案：查词多花 3 秒。这是痒点，不是痛点。真实痛点的判据是用户会主动去找解决方案——而没有人会搜索"怎么在知乎读长文少查词"。

### 2.3 三个来自项目自身的证据

1. 方案 §11.3 记录了"模型在做填空题，不是判断题"——核心输出依赖脆弱的模型行为
2. README 把"移动端覆盖不到"写成"已知边界 / 形态天花板"——把致命伤写成了体检报告
3. 首页第一屏退化成了文章列表——方案 §18.2 自己定的"三秒定生死"没有落地

### 2.4 被忽略的最大问题：产出没有回到知乎

赛道叫"知识炼金场"，评审表里明确要看"项目与知乎社区生态的契合点"。
但当前所有产出——笔记、导读、复盘、收藏体检——全部终止在用户本地的 localStorage 与导出的 md 文件里。

**没有一个字节回到知乎。这是一个工具，不是一个生态产品。**

---

## 三、改造方向：把"困惑"从私有消耗品变成社区公共品

### 3.1 那个不存在的痛点

知乎知道一篇回答有多少赞、多少收藏、多少评论。

知乎不知道：**读到第几段、多少人卡住了、卡在哪个词上。**

答主也永远不知道。评论区不是这个通道（评论是赞美和抬杠，不是困惑标记），产品里也没有"这篇里 X 概念我没懂"的按钮。

**困惑数据在知乎生态里是一个空洞。**

### 3.2 飞轮

```
读者卡住 →（划一下·已有动作）→ 卡点入账 → 同篇聚合
                                              ↓
                            答主看到「我的读者集体卡在这三个词」
                                              ↓
                                    补一段前置说明 / 置顶
                                              ↓
                          下一位读者的门槛降低（价值回到社区）
```

### 3.3 为什么这个方向成立

| # | 理由 |
|---|---|
| 1 | 不重做 P0，让同一个划选动作多赚一份价值——48 小时改得动 |
| 2 | 创造知乎不存在的数据类型，评审最看重"新"而非"全" |
| 3 | 精准命中赛道三句：理解 → 吸收 → 再次创造 |
| 4 | 把产品从工具升级为生态的一环，「社区契合点」「对社区用户的实际价值」两栏直接成立 |
| 5 | 化解移动端天花板：桌面端产生数据，所有端消费数据，价值不再依赖装插件 |
| 6 | 用事实消解羞耻感——"这里 1283 个人也卡住了"，比 IP 隐喻有效，因为它是事实不是安慰 |

### 3.4 一句话主张（替代现有 slogan）

> **你卡住的地方，也是所有人的卡点。**
>
> 划一下，你当场读懂；这一划，也让下一个读到这里的人不再卡住。

保留原有的"读不懂，划一下，当场讲明白"，但把落点从"你看懂了"改到"**你让所有人都少卡一次**"。

---

## 四、48 小时落地清单

### 4.1 首页三秒改版（最高优先级，改动最小，收益最大）

现在第一屏是：标题 + 三篇文章卡片。评委三秒内不知道这产品解决什么。

改为直接上洞察：

> 这篇回答被收藏了 3.2 万次。
> 但 6 成的人，在第 3 段就卡住了。
> 卡住他们的，是同一个词。

下面直接就是那篇回答 + 卡点可视化。改动集中在 `demo/js/app/home.js`。

### 4.2 浮层加一行卡点动作

划词解释完成后，浮层底部加一行：

> 这里我也卡了一下 · 1,283 人也卡在这

点一下 +1。零注册、零成本、不用多想。
改动集中在 `demo/js/app/popup.js`。

### 4.3 文章页卡点热力

复用现有侧栏 / 常驻入口，展示：

- 本篇卡点 TOP 3
- 各自的卡住人数
- 点击直接跳到该段落（复用 `store.js` 已有的 `anchorToRanges` 锚点回原文能力）

### 4.4 答主视角页（杀手锏）

标题就叫：**「你的读者，卡在这三个地方」**

输入任意知乎回答链接 → 显示这份回答的读者卡点报告 → 一键生成"前置说明"草稿（复用已有的导读生成能力）。

这一页让评委从"这是给读者的工具"升级为"这是给社区的基础设施"。

### 4.5 技术约束与取舍（必须诚实处理）

项目现有架构明确写着"没有数据库、没有自有服务器"。跨用户聚合需要持久存储。两条路：

- **保底（推荐先做）**：单篇卡点 = 预置 seed 数据 + 现场真实上报叠加。界面明确标注"演示环境数据"。
- **进阶（时间有富余再做）**：给 SCF 挂一个免费 KV / DB，实现真实跨用户聚合。

**不要为了聚合而造假数据。** 标注清楚比数据好看重要——评委问一句就穿帮的成本，远高于诚实标注的失分。

---

## 五、必须顺手修的三个线上问题

| # | 问题 | 证据 | 修法 |
|---|---|---|---|
| 1 | 返回结构偶尔多包一层 | 实测「的」返回 `{"answer":{"definition":...}}`，而 `normalizeAsk` 只读 `r.definition` → 浮层空白 | `api.js` 加 `const r0 = r.answer \|\| r` 后统一取字段 |
| 2 | 冷启动 26.8 秒 | 实测首次 26.8s，随后 1.0s | 页面加载时预热一次；加载态文案要诚实 |
| 3 | 后端无非概念兜底 | 实测「的」被正常回答 | `/ask` 加虚词判定，返回 `is_concept:false`（前端字段已预留，目前是死字段） |

---

## 六、明确不要做的事

- 不要再加新功能。功能已经够多了，问题是主路径不够锋利。
- 不要把知乎非官方 API 变成核心依赖。
- 不要做"一键发布到知乎"——官方接口只读，这是硬边界。
- 不要为了演示效果编造聚合数据而不标注。
- 不要在移动端工程上花时间——桌面端产数据、所有端消费数据，是这个方向自带的解法。

---

## 七、验收标准

1. 评委打开 Demo 三秒内，知道这产品解决什么问题（首屏有洞察，不是功能列表）。
2. 划一下 → 解释 → 看到"多少人也卡在这"，三步之内完成，无需注册。
3. 存在一个答主视角的页面，展示"读者卡点报告"。
4. 全站所有产出不再止于本地——至少有一条链路回到知乎社区。
5. `npm run check` 与 `test:core` / `test:hub` / `test:api` 全过。
6. 演示中出现的每一个数字，都能说清它从哪来。
