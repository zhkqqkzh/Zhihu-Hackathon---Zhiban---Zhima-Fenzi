# 项目复查报告 · 知伴 + 个人主页

复查时间：2026-09-12 晚
复查方式：全部为**真实调用/真实渲染**（线上接口实测、无头浏览器截图、本地测试套件、逐文件哈希比对），不是读代码推测。

---

## 结论先说

**知伴项目：行。** 上一份《问题清单.md》里的 8 条，7 条已修复并**在线上验证通过**，剩下 1 条是文档里的一个坏链接。

**个人主页：能用，但有三处自己打自己脸的地方要收。** 部署与内容都正常，问题都在细节。

---

## 一、知伴：对着上一份问题清单逐条复核

| 编号 | 上一份的问题 | 本次实测 | 状态 |
|---|---|---|---|
| P0-3 | 线上 Demo 地址无记录 | README §8.2 已写入；实测 `https://z.toply.top/zhiban-demo/` → 200，7 个静态资源（js/css）全部 200 | 已解决 |
| P0-1 | 返回结构漂移被判失败 | `core/explain.js` 新增解包；实测 4 次 `/ask` 全部为平铺结构，未再出现 `{"answer":{…}}` | 已解决 |
| P0-2 | 第三层非概念拦截是死代码 | 实测：`的` → `{"is_concept":false}`（193ms）；`张一鸣` → `{"is_concept":false}`（575ms）；`梯度下降`/`损失函数` → 正常返回四字段 | **已解决** |
| P1-1 | 幽灵标记率超标（6.7%） | 线上 `/prescan` 三篇实测：幽灵 0 个，**幽灵率 0.0%** | 已解决 |
| P1-2 | 词表混入通用词 | 清洗后 `函数 / 图像 / 参数 / 损失` 被正确丢弃，保留 `梯度下降 / 梯度 / 偏导数 / 导数 / 变化率 / 学习率 / 极限` | 已解决 |
| P1-3 | 预扫描慢且无提示 | `demo/js/app/prescan.js` 已加常驻提示「正在扫描全文概念…」（`pointer-events:none`，不影响划词） | 已解决 |
| P2-1 | 插件无构建产物 | README §8.3 已写清三步（`npm install` → `npm run build:ext` → 加载 `extension/`） | 已解决 |
| P2-2 | 首屏三秒失效 | 首屏截图确认：第一屏就是「8,432 人赞同 / 读到第 2 段 1,283 人卡住 / 卡住他们的是同一个词：链式法则」+ 卡点 TOP3 | 已解决 |

### 全量实测数据

| 项 | 实测结果 |
|---|---|
| 线上 demo 与本地 HEAD | **15/15 文件逐字节哈希一致**（说明部署不是旧版） |
| 静态资源 | index.html / router / home / creator / api / popup / sidebar / guide / store / runtime / ui / prescan / stuck / explain 全部一致 |
| `GET /ping` | 200 `{"ok":true}` |
| `POST /ask` 流式（线上默认路径） | 200、`text/event-stream`、**首字节 0ms、总耗时 610–642ms** |
| `POST /ask` 非流式 | 结构平铺、字段完整、非概念正确短路 |
| 错误分支 | 空参数 → 400 `{"error":"term 和 context 均为必填字符串"}`；未知路径 → 404 |
| 本地测试套件 | check-syntax **61 文件 0 失败**、check-imports ok、core **127**、hub **23**、api **22**、**e2e 48** —— 全部 0 失败 |
| 首屏渲染（1440×1100 无头截图） | 卡点洞察三句话 + TOP3 + 三篇文章卡片，层次正常 |
| 答主视角 `#/creator` | 「你的读者，卡在这三个地方」+ 链接输入框 + 生成入口，正常 |
| 个人中心 `#/profile` | 三张模块卡渲染正常，空状态诚实（「暂无推荐，先去读几篇」「还没有学习记录」） |

---

## 二、知伴：还没解决的

### 1. 【该修】README 里挂着一个坏链接

README §8.2 写着：

> <https://zhihu.toply.top/> 独立域名站点，已建站并绑定域名，**待 DNS 解析到本机后启用**

实测这个域名**不是"待启用"，是"坏的"**：

| 检查 | 结果 |
|---|---|
| HTTPS 证书 | **自签证书**（subject / issuer 均为空，有效期 2026-03-06 → 2036-03-03），浏览器会直接拦「您的连接不是私密连接」 |
| 页面返回 | **403 Forbidden**（nginx，146 字节） |

也就是说，**任何人（包括评委）点这个链接，先吃到证书警告，再吃到 403**。这是整个交付里唯一一个"外部可见的坏东西"。

两个选择，二选一：
- **修**：给站点配上正常证书 + 设好网站根目录，让它真能打开；
- **删**：从 README 里去掉这一行，只留 `https://z.toply.top/zhiban-demo/` 这一个可用地址。

### 2. 【提醒】冷启动

`/ask` 非流式首次调用仍会出现长时间等待（上一份实测冷启动 26.8 秒）。线上走的是流式、且 `main.js` 里已加了 `warmup()` 预热，所以**不影响正常演示**。但建议：**演示前 5 分钟先打开一次 demo**，让 SCF 实例热起来，别把第一次点击留给评委。

### 3. 【已知，不用动】移动端

形态天花板，README §一 已如实声明。保持诚实即可，不要为了"看起来全覆盖"去加移动端。

---

## 三、个人主页（toply.top）复查

页面标题就是「迟遇 | 个人主页」，源码在 `D:\project\个人博客`（Vue 3 + Vite，hash 路由）。

### 先说好的

| 项 | 实测 |
|---|---|
| 部署新鲜度 | 线上 bundle（`index-B3NKDaT2.js`）与本地 `dist/` **逐字节一致**，且包含最新的「website 模式下显示 Gitee 按钮」修复 |
| `index.html` | 线上与本地只差换行符（CRLF/LF），**内容完全一致** |
| `projects.json` | 线上与本地哈希一致（356B，1 条作品） |
| 外链 | 掌中星辰 `https://z.toply.top` 200、Gitee 仓库 200、GitHub 主页 200、Gitee 主页 200 |
| 整页渲染 | 关闭动画后完整渲染：Hero / 编程经历 / 项目作品 / 相关文章 / 仓库 / 页脚，结构清晰、留白舒服 |

### 要改的三处

| # | 问题 | 证据 | 建议 |
|---|---|---|---|
| 1 | **数字打架** | Hero 写「完成项目 **8 个**」，但「项目作品」区只列出 **1 个**（掌中星辰），下面还写「3+作品正在优化中」 | 访客会怀疑数字。要么把已完成的作品补进 `public/projects.json`，要么把 8 改成能对得上的数字 |
| 2 | **favicon 是 🍊** | `index.html` 的 `link rel="icon"` 用了橙色橘子 emoji | 和你「不要橘色 / 橘子元素」的一贯偏好冲突。建议换成 `💻`（与你的头像一致）或线条图标 |
| 3 | **无 JS 时整页不可见** | `.reveal { opacity: 0 }` 要等 IntersectionObserver 触发才显示。我首屏截图是**全白的**，加 `--force-prefers-reduced-motion` 才渲染出内容 | 给它加一层兜底，比如 `html.no-js .reveal { opacity: 1 }` 或首屏 Hero 不走 reveal。影响面：分享预览、爬虫、极慢网络下用户会先看到白屏 |

### 两个小提醒

- `Projects.vue` 里是 `fetch('/projects.json')`，**绝对路径**。现在站点挂在域名根目录没问题，但将来若挪到子目录（例如 `z.toply.top/某项目/`）就会 404。改成 `./projects.json` 更稳。
- 两篇微信文章的链接，我这边程序化访问被微信风控拦到了验证码页（`wappoc_appmsgcaptcha`），**无法判定**普通浏览器里能否正常打开——建议你手动点一次确认。

---

## 四、演示前清单

1. 处理 `zhihu.toply.top`：修好，或从 README 删掉
2. 演示前 5 分钟打开一次 demo（预热 SCF）
3. 个人主页那三处：数字对齐、换 favicon、加 reveal 兜底
4. 插件形态这一次**没有实测**（我跑的是 demo 站 + 端到端测试，没在真实知乎页面上加载扩展验证）——如果现场要用插件演示，建议自己先走一遍 `npm run build:ext` + 加载 `extension/`
5. 录一段「在真实知乎长文里用插件划词」的录屏或截图：Demo 站用的是内置范文，评委看到的排版是干净的；根场景（真实知乎页面的脏 DOM、公式、图片段落）只能靠插件形态体现，必须在演示里补上这一镜
6. 开场前先点侧栏「数据控制 → 载入示例数据」，并把某一篇读到结尾触发看山提问：看山提问、每周复盘、掌握率、Learning Hub 都依赖本地记录，全新浏览器打开时全是空状态（「这篇还没有能问你的问题」），先载入示例数据这些模块才是"有内容"的

---

## 附：本次没有覆盖的范围

- Chrome 插件在真实知乎页面上的注入效果（仅跑了项目自带的 e2e，对象是 demo 站）
- 移动端体验
- 智谱 API 的费用与限流上限（建议在智谱控制台设消费硬上限）
