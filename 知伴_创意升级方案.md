# 知伴 · 创意升级方案（2026-09-13 重写版）

> 用途：统一团队对「想法创意 + 当前真实进度」的共识，替代此前分散的诊断 / 计划文档。
> 范围：只谈创意表达与可信度，工程基线见 `产品说明文档.md`。
> 一句话结论（现状）：**最强创意「困惑数据 = 知乎空白」已经讲清楚了——首屏对比图、个人中心「我的贡献」、答主视角三处都已落地。现在卡在两件上线动作：① P1 真实内容代码写好了却没上线（线上 `/search` 还是 404，且要配 secret）；② C1 飞轮持久化代码已就位（`scf/stuck-store.js` 可插拔存储）但未启用——线上未配 `STUCK_REDIS_URL`，仍走进程内存、跨实例即丢。把这两件补上，整条叙事从首屏到个人视角才真正「眼见为实」。**

---

## 0. 当前创意诊断（按代码 + 线上实测，2026-09-13）

### 真洞察（保留，别动）

| # | 资产 | 为什么值钱 |
|---|---|---|
| S1 | **困惑数据 = 知乎空白** | 知乎知道赞 / 收藏 / 评论，但不知道「读者读到第几段、卡在哪个词」。真空白、真新鲜，不是「又一个划词工具」 |
| S2 | **理解 → 吸收 → 再次创造** | 三段论精准命中知识炼金场原话，零包装成本 |
| S3 | 口号「你有没有一篇收藏了、却永远没读完的知乎长文」 | 情绪钩子，人人对号入座 |
| S4 | 读者 ↔ 答主双向闭环 | 比单点工具高一个维度 |
| S5 | 三源标注机制（演示 / 本地 / 社区） | 出发点是对诚实的刻意设计，方向对 |

### 硬伤（只列还没修的，已落地的移出）

| # | 问题 | 实测状态 | 后果 |
|---|---|---|---|
| **C1** | 飞轮命根子「社区」代码已支持持久化，但**线上未启用**：`scf/stuck-store.js` 可插拔存储（默认进程内存，配 `STUCK_REDIS_URL` 即 Redis 持久 / 冷启动与跨设备共享，失败自动降级内存），SCF 环境变量未配 `STUCK_REDIS_URL`，线上仍走内存 | 配了才跨实例 / 跨设备互见；不配则同热实例内 `report`→`top` 能攒起、轮换即丢（与旧版一致）；本地 `profile.js:250` 仍诚实写「只给本机真实上报口径」 | 首页 / 个人中心的「N 人也卡住」来自 seed（已诚实标「演示环境数据」）；「你划一下让下个人少卡一次」要配 `STUCK_REDIS_URL` 后才从 story 变 fact |
| **P1（上线）** | 真实内容**代码已闭环却未上线**：`demo/js/app/knowledge-source.js` + 后端 `/search` 真连 `https://developer.zhihu.com/api/v1/content/zhihu_search`，但线上 `/search` 实测 **404**（新 `index.js` 没重新打包上传 SCF）；且需 SCF 环境变量 `ZHIHU_ACCESS_SECRET`，未配则 `_degraded` 返回空 | 本地实测：`knowledge-source.js` 已把官方搜索结果归一化成「真实知乎」文章并接入文章页 / 首页；硬性降级（接口失效 / 空结果一律返回空，绝不把演示数据冒充真实）已就位 | 评委上手仍是内置 3 篇手写范文（ML 文章，`voteupCount:8432` 写死），不是真实知乎长文；「真能解决真实问题」立不彻底 |
| **C5（数据层）** | 答主视角页已实现（`creator.js`），但「别台设备经 `/stuck` 上报的卡点」依赖 C1 持久化才真有跨设备数据 | `creator.js:105` 已调 `mergeStuck(... community)` 做社区聚合，但 community 当前基本为空（C1 代码已就位、线上未启用持久化） | 演示里答主看到的卡点报告，数据仍偏本机，社区聚合是 story（配 `STUCK_REDIS_URL` 后变 fact） |

> 已落地、移出硬伤清单：C2/C3 首屏对比图 ✅、C4 口号过度承诺 ✅、C6 个人中心 ✅。

---

## 1. 主视觉：对比图草模（Hero 视觉，已落地）

> 全项目最强视觉创意，已提为第一屏（`home.js` `buildInsightChildren`）。左半「知乎知道的结果」用灰、静态数字；右半「知乎不知道的过程」用品牌色高亮，中间裂痕线点破「知乎知道结果，不知道过程」。数字当前带「示例数据」标签，**C1 接通后右半数字应现场真实累计**。

```
┌──────────────────────────────────────────────────────────────────────┐
│                 知伴 · 主视觉（首屏 Hero，已落地）                       │
├───────────────────────────────┬──────────────────────────────────────┤
│   知乎「知道」的               │   知乎「不知道」的  ← 知伴补上这条      │
│   👍 8,432 赞（示例数据）       │   📍 读者读到第 2 段就停了             │
│   💬 326 评论                  │   🔴 卡在「链式法则」的人都聚在这       │
│                                │   👥 N 人也卡在这（示例数据 → C1后变真）│
├───────────────────────────────┼──────────────────────────────────────┤
│   热闹，但说的是「结果」        │   安静，但说的是「过程」——             │
│                                │   知识炼金场「吸收」断点，就在这        │
└───────────────────────────────┴──────────────────────────────────────┘
```

设计规范不变：左灰右亮、裂痕隐喻、右底行动句「知伴，让你卡住的地方，也成为别人读得懂的地方」。

---

## 2. 口号三版备选（已落地，文案层）

| 版本 | 文案 | 适用 |
|---|---|---|
| **主推** | 你有没有一篇收藏了、却永远没读完的知乎长文？多半不是因为你懒——是读到一半卡在一个词上，然后就关了。知伴做的，就是让本来会放弃的人，读完。 | 视频开场、文档首段 |
| **A（克制）** | 很多人收藏了却没读完知乎长文——往往不是懒，是卡在一个词上。知伴让你读完。 | 海报主标（避「多半」过度承诺） |
| **B（突出空白）** | 知乎知道你的赞、收藏、评论；却不知道你读到第几段就放弃了。知伴补上这条。 | PPT 核心页、对比图右半 |
| **C（行动）** | 下次读长文卡住，别关。划一下，知伴当场讲明白——还告诉你好多人也卡这。 | Demo 引导、短视频字幕 |

---

## 3. 视频脚本修订版（30–60s，诚实口径更新）

| 镜号 | 画面 | 台词 / 字幕 |
|---|---|---|
| 1 | 主视觉对比图逐块亮起，裂痕线落下 | 「知乎知道你的赞、收藏、评论；却不知道你读到第几段就放弃了。」 |
| 2 | 主推口号前半 | 「你有没有一篇收藏了、却永远没读完的知乎长文……」 |
| 3 | 真实知乎长文滚动到术语处，划选「链式法则」；浮层逐字流式出解释 | 「划一下，它就在这篇回答的语境里讲明白——不是词典义，是这段里的意思。」 |
| 4 | 点「卡了一下」→ 侧栏「本篇卡点 TOP3」，**三源标注来源** | 「你卡住的地方，也是很多人的卡点。（演示环境数据 / 你的上报 / 社区上报 如实标注）」 |
| 5 | 切「答主视角」tab：读者卡点报告 + 一键生成「前置说明」草稿 | 「答主看得到读者卡在哪——补上这一句，下一任读者就不再卡。」 |
| 6 | 个人中心「我的贡献」下钻（真实聚合数字 / 本机口径） | 「问过的概念自己攒成知识图谱，每周告诉你最该补什么。」 |
| 7 | 回主视觉对比图收尾 | 「知伴，让你卡住的地方，也成为别人读得懂的地方。」 |

**录制原则**
1. 预热：录制前打开目标知乎页静置 10s（SCF 冷启动约 27s，热实例解释 <1s）。
2. 镜头 3 选含「梯度下降 / 链式法则 / 正则化」的真实回答；浮层逐字出完再切镜。
3. **镜头 4 诚实口径（替代旧「摆拍」备注）**：界面本就三源标注。若 P1 已配 secret 上线，可现场搜真实知乎长文演示（镜 3 直接用真实内容）；若 C1 未接通，所有「社区上报」数字**口播「演示环境数据」**，绝不把 seed 标成社区上报。
4. 镜头 5 同页切 tab，点第一条卡点跳回原文高亮，一镜到底最有说服力。
5. 镜头 6 个人中心需先有数据——真实问 2–3 个概念，或点「数据控制 → 载入示例数据」。
6. 全程不出现「我们的特色 / 技术架构」字样，让体验自己说话。

**现场被追问备用口径**
- 「数据是真的吗」：演示数据全程标注来源；社区数字来自真实匿名上报（概念名 + 段号），C1 持久化接通后可现场两台设备互见。
- 「为什么不用官方直答」：100 次 / 日撑不住高频划词；我们要「概念 + 本段语境」的精准流式解释，自建 GLM 才可控可流式。
- 「隐私呢」：阅读数据全在本机，唯一上传的是卡点匿名计数；这是刻意取舍的单机隐私优先版。

---

## 4. 创意升级清单（刷新：已做 / 待做）

| 诊断 | 状态 | 动作 |
|---|---|---|
| **C1 飞轮半真** | 🟡 代码已就位 / ⏳ 待启用 | `/stuck` 已接可插拔持久存储（`scf/stuck-store.js`：默认内存、配 `STUCK_REDIS_URL` 即 Redis 持久，失败自动降级），**不动前端、不动体验**；只差线上开 Redis 并配 `STUCK_REDIS_URL` + 重新打包，评委划一下、换台设备即可见数字真实涨 |
| **C2/C3 第一眼平庸 / 无符号** | ✅ 已落地 | 首屏对比图已上线并作主视觉，保持 |
| **C4 过度承诺** | ✅ 已就位 | 主推版保留；海报 / 短句用 A 版「往往」 |
| **C5 闭环空转** | ✅ 页面已落地 / ⏳ 数据等 C1 | 答主视角页（`creator.js`）已实现；跨设备卡点数据等 C1 持久化 |
| **C6 个人中心脱钩** | ✅ 已落地 | `profile.js` 已实现「我的贡献 / 卡点地图 / 时间线」，诚实标注 |
| **P1 真实内容** | 🟡 代码闭环 / ⏳ 待上线 | 两步：① 重新打包 `scf/index.js` 上传 SCF（让线上有 `/search`）；② 在 SCF 环境变量配 `ZHIHU_ACCESS_SECRET`（知乎开发者后台的 access token）。未配则降级空，前端仍显示内置 3 篇 |

---

## 5. 提交前创意自检（刷新）

- [x] 第一屏是「对比图 Hero」，不是功能说明列表
- [x] 三个口号版本在文档 / PPT / 海报各自就位、同源不打架
- [x] 视频脚本已删除「摆拍社区数据」备注，数据全部如实标注
- [x] 「社区上报」字眼一律如实标注（三源分离已落地，绝不用 seed 冒充社区）；C1 代码已就位，配 `STUCK_REDIS_URL` 后社区数字变真跨设备
- [x] 对比图作为主视觉出现在封面、PPT 核心页、视频开场
- [x] 答主视角演示已实现（标注跨设备数据依赖 C1）
- [x] 口号避免过度承诺（「往往」而非「多半」在短句中）
- [x] 个人中心首屏是「个人困惑 → 社区同卡 → 我的贡献」叙事，不是功能卡网格
- [x] 个人中心「我的贡献」数字三源标注，绝无 seed 冒充社区；C1 未接通时不编数字
- [x] 视频镜 6 下钻「我的贡献」看到真实聚合（本机口径）/ 待 C1 变跨设备

---

## 6. 个人中心优化（已落地，更新为实际实现）

> 原方案 §6 的目标已全部实现于 `demo/js/app/profile.js`（相较旧版 +475 行）：
> - 首屏从功能卡网格改为「你卡住的地方，也是别人的卡点」叙事（`renderHomeModule`，首句即 §6.2 核心重构句）；
> - 新增「我的贡献」（贡献总览 / 卡点地图 / 时间线，可下钻）；
> - 收藏体检重定位为「哪些概念你其实没真懂」；
> - 功能入口退为次级导航。
>
> **诚实红线已生效**：`profile.js:250` 写明「C1 未接通，不编造被看到 / 帮到数字，只给本机真实上报口径」。这一模块只有在 **C1（`/stuck` 持久化）修复后**，「我的贡献」的社区数字才从本机口径变真实跨设备聚合——与 C1 强绑定，是让飞轮从 story 变 fact 的最后一环。

首屏草模（已实现，仅数字真实度待 C1）：

```
┌──────────────────────────────────────────────────────────────┐
│  🧭 个人中心（已落地）                                          │
├──────────────────────────────────────────────────────────────┤
│  你卡住的地方，已经有 N 个人也卡住了——而你刚才划的那一下，       │
│  正在帮下一个读到的人少卡一次。                                  │
├──────────────────────────────────────────────────────────────┤
│  🔴 你最常卡住的 3 个概念（标来源：你的上报 / 社区上报）          │
├──────────────────────────────────────────────────────────────┤
│  🤝 我的贡献（C1 接通前只给本机真实上报口径，不编数字）          │
│  [贡献总览] [卡点地图] [时间线]                                 │
├──────────────────────────────────────────────────────────────┤
│  [收藏体检] [推荐阅读] [学习数据] [我的笔记] [学习复盘] [足迹]  │
└──────────────────────────────────────────────────────────────┘
```

---

## 7. 落地状态追踪（2026-09-13 重写实测）

> 本表基于代码审查 + 线上实测，逐条对照 §0 诊断。**打勾 = 已落地可演示；待办 = 还没改 / 没上线，是当前「第一眼 / 可信度」还差的那口气。**

### 7.1 已落地（创意主线基本跑通）

| 诊断 | 落地证据 | 状态 |
|---|---|---|
| **C2/C3 主视觉首屏** | `home.js` `buildInsightChildren`：左「知乎知道（赞/评论/结果）」vs 右「知乎不知道（读到第几段/卡在哪个词）」，中间裂痕点破「知乎知道结果，不知道过程」；数字带「示例数据」标签 | ✅ 已落地 |
| **C4 口号过度承诺** | 主推版保留；A 版「往往」就位 | ✅ 已就位（文案层） |
| **C6 个人中心** | `profile.js` 首屏叙事 +「我的贡献 / 卡点地图 / 时间线」+ 收藏体检重定位 + 功能入口退次级；诚实红线生效（`:250`） | ✅ 已落地 |
| **C5 答主视角（页面）** | `creator.js`：输入知乎回答链接 → 读者卡点 TOP3 报告 → 一键生成「前置说明」草稿，可跳回原文 | ✅ 页面已落地（数据层待 C1） |
| **S5 三源标注** | `stuck.js` seed / marks / community 三层叠加，每处数字标来源，无 seed 冒充 | ✅ 已落地 |
| **P1 真实内容（代码层）** | `knowledge-source.js` 归一化真实知乎文章并接入文章页 / 首页；后端 `/search` 真连 `developer.zhihu.com` 搜索；硬性降级就位 | 🟡 代码闭环（**线上未部署 + 缺 secret**） |
| **C1 飞轮持久化（代码层）** | `scf/stuck-store.js` 可插拔存储：默认进程内存（旧行为），配 `STUCK_REDIS_URL` 即 Redis 持久 / 跨设备共享，Redis 失败自动降级内存、契约不变；`scf/index.js` `/stuck` 逐字段契约不变，前端零改动 | 🟡 代码闭环（**未配 `STUCK_REDIS_URL` 仍走内存**） |
| **测试基线** | check 67 / core 159 / hub 23 / api 28 / e2e 73（本机全绿，含 C1 存储断言；语法门禁已覆盖 `scf/`，`stuck-store.js` 纳入校验） | ✅ 健康 |

### 7.2 还差什么（决定「经得起问」）

| # | 没改 / 没上线什么 | 实测状态 | 后果 | 修法 |
|---|---|---|---|---|
| **C1 持久化上线** | 代码已支持持久化（`scf/stuck-store.js` 可插拔），但 SCF 环境变量未配 `STUCK_REDIS_URL`，线上仍走进程内存 | 配了才跨冷启动 / 跨设备共享；不配则行为与旧版一致 | 不配时「社区飞轮」仍只在同热实例内成立，个人中心社区数字仍偏本机口径 | ① 开 Redis 免费额度；② SCF 环境变量配 `STUCK_REDIS_URL`（`redis://...`）；③ 重新打包上传（含 `npm install redis`） |
| **P1 上线** | 新 `index.js`（含 `/search`）未重新打包上传 SCF；SCF 环境变量缺 `ZHIHU_ACCESS_SECRET` | 线上 `/search` 实测 404；即使上传，无 secret 也会 `_degraded` 返回空 | 评委上手仍是内置 3 篇手写范文，不是真实知乎长文 | ① 打包上传 `scf/index.js`；② 配 `ZHIHU_ACCESS_SECRET`（知乎开发者 token） |
| **同步部署** | 工作区改动已提交（`e2a6c5a` / `fc5fbce`），测试基线本机全绿；但线上 SCF 与静态站仍是旧版 | `git status` 干净、可复现；线上未同步 | 本地状态已固定，但线上不更新则 ① / ② 都白做，评委看到的仍是旧行为 | 重新打包上传 SCF（含 `/search` + C1）+ 上传 `demo/` 到静态站 |

### 7.3 下一步行动（按性价比排序）

1. **紧急 · P1 上线**：打包上传 `scf/index.js` 让线上有 `/search`；在 SCF 环境变量配 `ZHIHU_ACCESS_SECRET`。这一步让评委上手是**真实知乎长文**，直接把「真能解决真实问题」立住。
2. **高性价比 · C1 持久化上线**：代码已就绪（`scf/stuck-store.js` 可插拔存储），只差在 SCF 环境变量配 `STUCK_REDIS_URL` 并重新打包上传（含 `npm install redis`）。让飞轮从 story 变 fact，个人中心「我的贡献」社区数字变真，答主视角跨设备卡点报告变真。不动前端、不动体验。
3. **收尾 · 同步部署**：工作区改动已提交、测试基线本机全绿（check 67 / core 159 / hub 23 / api 28 / e2e 73）；剩按 ① / ② 重新打包上传 SCF，并把 `demo/` 同步到静态站。

### 7.4 一句话结论

> **第一眼已经从「看不懂解决什么」变成「哦，它补的是知乎不知道的那半」——这一关过了。** 剩两件都不致命，但决定评委是「觉得好看」还是「问不倒」：
> - **P1 上线** = 让评委上手的是真实知乎长文，不是范文（当前代码已好，只差部署 + 配 secret）；
> - **C1 持久化** = 让最锋利的「社区飞轮」叙事从 story 变 fact（代码已就绪，性价比最高，只差配 `STUCK_REDIS_URL`）。
>
> 两件补上后，配合已落地的首屏对比图 + 个人中心「我的贡献」+ 答主视角，整条「困惑数据 = 知乎空白」叙事从首屏到个人视角**完全闭环、眼见为实**。

---

> 本方案替代并整合：`问题清单.md`、`真实痛点改造方案.md`、`项目复查报告_2026-09-12.md`、`官方要求对照与功能增减建议.md`、`黑客松作品_剩余问题.md`、`知伴_黑客松冲刺计划书.md`、`知伴_定位与落地推广方案.md`、`演示视频脚本.md`。保留 `产品说明文档.md` 作为提交必交件。
