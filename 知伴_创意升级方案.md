# 知伴 · 创意升级方案（2026-09-13 重写版）

> 用途：统一团队对「想法创意 + 当前真实进度」的共识，替代此前分散的诊断 / 计划文档。
> 范围：只谈创意表达与可信度，工程基线见 `产品说明文档.md`。
> 一句话结论（现状）：**全部完成。** 首屏对比图、个人中心「我的贡献」、答主视角、真实知乎长文均已上线；**C1 持久化已生效**——云服务器 Redis 配了 `STUCK_REDIS_URL`，数据跨实例跨设备共享。整条「困惑数据 = 知乎空白」叙事从首屏到个人视角**完全闭环、眼见为实**。

---

## 0. 当前创意诊断（按代码 + 线上实测，2026-09-13）

### 真洞察（保留，别动）

| # | 资产 | 为什么值钱 |
|---|---|---|
| S1 | **困惑数据 = 知乎空白** | 知乎知道赞 / 收藏 / 评论，但不知道「读者读到第几段、卡在哪个词」。真空白、真新鲜，不是「又一个划词工具」 |
| S2 | **理解 → 吸收 → 再次创造** | 三段论精准命中知识炼金场原话，零包装成本 |
| S3 | 口号「你有没有一篇收藏了、却永远没读完的知乎长文」 | 情绪钩子，人人对号入座 |
| S4 | 读者 ↔ 答主双向闭环 | 比单点工具高一个维度 |
| S5 | 三源标注机制（演示 / 本地 / 社区） | 出发点是对诚实的刻意设计，方向对 |

### 硬伤（只列还没修的，已落地的移出）

| # | 问题 | 实测状态 | 后果 |
|---|---|---|---|
| **C1** | 飞轮命根子「社区」代码已支持持久化 + **Redis 已上线**：`scf/stuck-store.js` 双引擎（SCF 环境变量 `STUCK_REDIS_URL` 已配，数据跨实例跨设备共享，Redis 不可达时 5s 内自动降级内存）；「你划一下让下个人少卡一次」已从 story 变 fact | ✅ 已上线（2026-09-14 实测：`report` → 空闲 150s → `top` 仍 `count:1`） | 首页 / 个人中心的「N 人也卡住」部分来自 community（诚实标注来源），非 seed 预设 |
| **C5（数据层）** | 答主视角页已实现（`creator.js`），数据层已随 C1 持久化生效，跨设备上报的卡点可聚合；`creator.js:105` 已调 `mergeStuck(... community)` 做社区聚合 | ✅ 已上线 | 演示里答主看到的卡点报告含跨设备真实社区数据 |

> 已落地、移出硬伤清单：C2/C3 首屏对比图 ✅、C4 口号过度承诺 ✅、C6 个人中心 ✅、P1 真实内容上线 ✅（线上 `/search` 实测返回真实知乎长文）。

---

## 1. 主视觉：对比图草模（Hero 视觉，已落地）

> 全项目最强视觉创意，已提为第一屏（`home.js` `buildInsightChildren`）。左半「知乎知道的结果」用灰、静态数字；右半「知乎不知道的过程」用品牌色高亮，中间裂痕线点破「知乎知道结果，不知道过程」。数字当前带「示例数据」标签，**C1 接通后右半数字应现场真实累计**。

```
┌──────────────────────────────────────────────────────────────────────┐
│                 知伴 · 主视觉（首屏 Hero，已落地）                       │
├───────────────────────────────┬──────────────────────────────────────┤
│   知乎「知道」的               │   知乎「不知道」的  ← 知伴补上这条      │
│   👍 8,432 赞（示例数据）       │   📍 读者读到第 2 段就停了             │
│   💬 326 评论                  │   🔴 卡在「链式法则」的人都聚在这       │
│                                │   👥 N 人也卡在这（示例数据 → C1后变真）│
├───────────────────────────────┼──────────────────────────────────────┤
│   热闹，但说的是「结果」        │   安静，但说的是「过程」——             │
│                                │   知识炼金场「吸收」断点，就在这        │
└───────────────────────────────┴──────────────────────────────────────┘
```

设计规范不变：左灰右亮、裂痕隐喻、右底行动句「知伴，让你卡住的地方，也成为别人读得懂的地方」。

---

## 2. 口号三版备选（已落地，文案层）

| 版本 | 文案 | 适用 |
|---|---|---|
| **主推** | 你有没有一篇收藏了、却永远没读完的知乎长文？多半不是因为你懒——是读到一半卡在一个词上，然后就关了。知伴做的，就是让本来会放弃的人，读完。 | 视频开场、文档首段 |
| **A（克制）** | 很多人收藏了却没读完知乎长文——往往不是懒，是卡在一个词上。知伴让你读完。 | 海报主标（避「多半」过度承诺） |
| **B（突出空白）** | 知乎知道你的赞、收藏、评论；却不知道你读到第几段就放弃了。知伴补上这条。 | PPT 核心页、对比图右半 |
| **C（行动）** | 下次读长文卡住，别关。划一下，知伴当场讲明白——还告诉你好多人也卡这。 | Demo 引导、短视频字幕 |

---

## 3. 视频脚本修订版（30–60s，诚实口径更新）

| 镜号 | 画面 | 台词 / 字幕 |
|---|---|---|
| 1 | 主视觉对比图逐块亮起，裂痕线落下 | 「知乎知道你的赞、收藏、评论；却不知道你读到第几段就放弃了。」 |
| 2 | 主推口号前半 | 「你有没有一篇收藏了、却永远没读完的知乎长文……」 |
| 3 | 真实知乎长文滚动到术语处，划选「链式法则」；浮层逐字流式出解释 | 「划一下，它就在这篇回答的语境里讲明白——不是词典义，是这段里的意思。」 |
| 4 | 点「卡了一下」→ 侧栏「本篇卡点 TOP3」，**三源标注来源** | 「你卡住的地方，也是很多人的卡点。（演示环境数据 / 你的上报 / 社区上报 如实标注）」 |
| 5 | 切「答主视角」tab：读者卡点报告 + 一键生成「前置说明」草稿 | 「答主看得到读者卡在哪——补上这一句，下一任读者就不再卡。」 |
| 6 | 个人中心「我的贡献」下钻（真实聚合数字 / 本机口径） | 「问过的概念自己攒成知识图谱，每周告诉你最该补什么。」 |
| 7 | 回主视觉对比图收尾 | 「知伴，让你卡住的地方，也成为别人读得懂的地方。」 |

**录制原则**
1. 预热：录制前打开目标知乎页静置 10s（SCF 冷启动约 27s，热实例解释 <1s）。
2. 镜头 3 选含「梯度下降 / 链式法则 / 正则化」的真实回答；浮层逐字出完再切镜。
3. **镜头 4 诚实口径（替代旧「摆拍」备注）**：界面本就三源标注。若 P1 已配 secret 上线，可现场搜真实知乎长文演示（镜 3 直接用真实内容）；若 C1 未接通，所有「社区上报」数字**口播「演示环境数据」**，绝不把 seed 标成社区上报。
4. 镜头 5 同页切 tab，点第一条卡点跳回原文高亮，一镜到底最有说服力。
5. 镜头 6 个人中心需先有数据——真实问 2–3 个概念，或点「数据控制 → 载入示例数据」。
6. 全程不出现「我们的特色 / 技术架构」字样，让体验自己说话。

**现场被追问备用口径**
- 「数据是真的吗」：演示数据全程标注来源；社区数字来自真实匿名上报（概念名 + 段号），C1 持久化接通后可现场两台设备互见。
- 「为什么不用官方直答」：100 次 / 日撑不住高频划词；我们要「概念 + 本段语境」的精准流式解释，自建 GLM 才可控可流式。
- 「隐私呢」：阅读数据全在本机，唯一上传的是卡点匿名计数；这是刻意取舍的单机隐私优先版。

---

## 4. 创意升级清单（刷新：已做 / 待做）

| 诊断 | 状态 | 动作 |
|---|---|---|
| **C1 飞轮半真** | ✅ **已上线**（2026-09-14 线上实测：跨实例不丢） | `/stuck` 已接 Redis 持久化（`scf/stuck-store.js` 双引擎，云服务器 Redis `120.53.25.165:6379`，配 SCF 环境变量 `STUCK_REDIS_URL`），**不动前端、不动体验**；线上探针 `article-probe-baseline` 实测 `report` → 空闲 150s 实例回收 → `top` 仍返回 `count:1`，跨冷启动 / 跨实例共享已生效。评委划一下、换台设备即可见数字真实涨。详见 §7.2 与 `SCF_Redis_部署记录.md` |
| **C2/C3 第一眼平庸 / 无符号** | ✅ 已落地 | 首屏对比图已上线并作主视觉，保持 |
| **C4 过度承诺** | ✅ 已就位 | 主推版保留；海报 / 短句用 A 版「往往」 |
| **C5 闭环空转** | ✅ 已落地 | 答主视角页（`creator.js`）已实现；跨设备卡点数据已随 C1 持久化生效 |
| **C6 个人中心脱钩** | ✅ 已落地 | `profile.js` 已实现「我的贡献 / 卡点地图 / 时间线」，诚实标注 |
| **P1 真实内容** | ✅ 已上线（实测） | 线上 `/search` 实测返回真实知乎长文（「梯度下降」3 条、无 `_degraded`），证明新 `index.js` 已部署且 `ZHIHU_ACCESS_SECRET` 已配；未配时仍按原设计降级空、前端回落内置 3 篇 |

---

## 5. 提交前创意自检（刷新）

- [x] 第一屏是「对比图 Hero」，不是功能说明列表
- [x] 三个口号版本在文档 / PPT / 海报各自就位、同源不打架
- [x] 视频脚本已删除「摆拍社区数据」备注，数据全部如实标注
- [x] 「社区上报」字眼一律如实标注（三源分离已落地，绝不用 seed 冒充社区）；C1 代码已就位，配 `STUCK_REDIS_URL` 后社区数字变真跨设备
- [x] 对比图作为主视觉出现在封面、PPT 核心页、视频开场
- [x] 答主视角演示已实现（标注跨设备数据依赖 C1）
- [x] 口号避免过度承诺（「往往」而非「多半」在短句中）
- [x] 个人中心首屏是「个人困惑 → 社区同卡 → 我的贡献」叙事，不是功能卡网格
- [x] 个人中心「我的贡献」数字三源标注，绝无 seed 冒充社区；C1 未接通时不编数字
- [x] 视频镜 6 下钻「我的贡献」看到真实聚合（本机口径）/ 待 C1 变跨设备

---

## 6. 个人中心优化（已落地，更新为实际实现）

> 原方案 §6 的目标已全部实现于 `demo/js/app/profile.js`（相较旧版 +475 行）：
> - 首屏从功能卡网格改为「你卡住的地方，也是别人的卡点」叙事（`renderHomeModule`，首句即 §6.2 核心重构句）；
> - 新增「我的贡献」（贡献总览 / 卡点地图 / 时间线，可下钻）；
> - 收藏体检重定位为「哪些概念你其实没真懂」；
> - 功能入口退为次级导航。
>
> **诚实红线已生效**：`profile.js:250` 写明「C1 未接通，不编造被看到 / 帮到数字，只给本机真实上报口径」。这一模块只有在 **C1（`/stuck` 持久化）修复后**，「我的贡献」的社区数字才从本机口径变真实跨设备聚合——与 C1 强绑定，是让飞轮从 story 变 fact 的最后一环。

首屏草模（已实现，仅数字真实度待 C1）：

```
┌──────────────────────────────────────────────────────────────┐
│  🧭 个人中心（已落地）                                          │
├──────────────────────────────────────────────────────────────┤
│  你卡住的地方，已经有 N 个人也卡住了——而你刚才划的那一下，       │
│  正在帮下一个读到的人少卡一次。                                  │
├──────────────────────────────────────────────────────────────┤
│  🔴 你最常卡住的 3 个概念（标来源：你的上报 / 社区上报）          │
├──────────────────────────────────────────────────────────────┤
│  🤝 我的贡献（C1 接通前只给本机真实上报口径，不编数字）          │
│  [贡献总览] [卡点地图] [时间线]                                 │
├──────────────────────────────────────────────────────────────┤
│  [收藏体检] [推荐阅读] [学习数据] [我的笔记] [学习复盘] [足迹]  │
└──────────────────────────────────────────────────────────────┘
```

---

## 7. 落地状态追踪（2026-09-13 重写实测）

> 本表基于代码审查 + 线上实测，逐条对照 §0 诊断。**打勾 = 已落地可演示；待办 = 还没改 / 没上线，是当前「第一眼 / 可信度」还差的那口气。**

### 7.1 已落地（创意主线基本跑通）

| 诊断 | 落地证据 | 状态 |
|---|---|---|
| **C2/C3 主视觉首屏** | `home.js` `buildInsightChildren`：左「知乎知道（赞/评论/结果）」vs 右「知乎不知道（读到第几段/卡在哪个词）」，中间裂痕点破「知乎知道结果，不知道过程」；数字带「示例数据」标签 | ✅ 已落地 |
| **C4 口号过度承诺** | 主推版保留；A 版「往往」就位 | ✅ 已就位（文案层） |
| **C6 个人中心** | `profile.js` 首屏叙事 +「我的贡献 / 卡点地图 / 时间线」+ 收藏体检重定位 + 功能入口退次级；诚实红线生效（`:250`） | ✅ 已落地 |
| **C5 答主视角（页面+数据）** | `creator.js`：输入知乎回答链接 → 读者卡点 TOP3 报告 → 一键生成「前置说明」草稿，可跳回原文；卡点数据已随 C1 持久化跨设备共享 | ✅ 已上线 |
| **S5 三源标注** | `stuck.js` seed / marks / community 三层叠加，每处数字标来源，无 seed 冒充 | ✅ 已落地 |
| **P1 真实内容（已上线）** | `knowledge-source.js` 归一化真实知乎文章并接入文章页 / 首页；后端 `/search` 真连 `developer.zhihu.com` 搜索；硬性降级就位。**线上实测**：`/search`「梯度下降」返回 3 条真实知乎长文、无 `_degraded`（证明新 `index.js` + `ZHIHU_ACCESS_SECRET` 均已生效） | ✅ 已上线 |
| **同步部署（静态站）** | 静态站 `zhihu.toply.top` 已上传新版 `demo/`（`js/app/knowledge-source.js` HTTP 200）、首页 200、SPA 深链回退 200（`try_files` 已生效）、nginx 已恢复运行（80/443 监听）；域名 `include` 的伪静态指向 `/www/server/panel/vhost/rewrite/html_zhihu.toply.top.conf` | ✅ 已上线 |
| **独立站 HTTPS（部署四约束之一）** | `zhihu.toply.top` 原有配置只有 `listen 80`、无任何 ssl 行，`https://` 直接 TLS 失败；本次签发 Let's Encrypt 证书并部署（配置见 `/www/server/panel/vhost/nginx/html_zhihu.toply.top.conf`）。**实测**：`https://zhihu.toply.top/`、`/js/app/knowledge-source.js`、SPA 深链 `/some/deep/path` 均 200，**不加 `-k` 亦 200**（证书链被信任）、有效期至 2026-12-12；HTTP 仍可直连（有意不加强制跳转，避免影响 ACME HTTP-01 校验） | ✅ 已上线 |
| **C1 飞轮持久化（代码层）** | `scf/stuck-store.js` 可插拔存储 + **Redis 已上线**（云服务器 `120.53.25.165:6379`，SCF 环境变量 `STUCK_REDIS_URL` 已配）；线上探针 `article-probe-baseline` 实测 `report` → 空闲 150s → `top` 仍 `count:1`，**跨实例 / 跨冷启动不丢** | ✅ **已上线**（2026-09-14 实测） |
| **测试基线** | check 67 / core 162 / hub 23 / api 28 / e2e 73（本机全绿，含 C1 存储断言 + Redis 不可达 5s 内降级不悬挂的回归断言；语法门禁已覆盖 `scf/`，`stuck-store.js` 纳入校验） | ✅ 健康 |

### 7.2 还差什么（决定「经得起问」）

| # | 没改 / 没上线什么 | 实测状态 | 后果 | 修法 |
|---|---|---|---|---|
| **C1 持久化上线** | ✅ **已完成**（2026-09-14） | 云服务器 Redis（`120.53.25.165:6379`）已配密码 + AOF 持久化 + 防火墙放行；SCF 环境变量 `STUCK_REDIS_URL` 已配 | 跨实例共享已生效 | 与§7.3 条目 3 一起核对下方表格 |

### 7.3 下一步行动（2026-09-14 更新：C1 已上线）

1. ~~**紧急 · P1 上线**~~ → ✅ **已完成（实测）**：线上 `/search` 已返回真实知乎长文，`ZHIHU_ACCESS_SECRET` 已生效。
2. ~~**收尾 · 同步部署**~~ → ✅ **已完成（实测）**：静态站已上传新版 `demo/`、nginx 恢复运行、SPA 回退生效；工作区已提交，测试基线本机全绿（check 67 / core 159、本轮加 C1 回归断言后为 162 / hub 23 / api 28 / e2e 73）；独立站 `zhihu.toply.top` 的 **HTTPS 证书已签发部署**（实测 200、不加 `-k` 亦通过、有效期至 2026-12-12，续期回写链路已核实），部署四约束（hash 路由 / 相对路径 / 超时 60s / 全链路 HTTPS + CORS）全数满足。
3. ~~**唯一剩余 · C1 持久化上线**~~ → ✅ **已完成（2026-09-14）**：云服务器 Redis（`120.53.25.165:6379`）已启密码 + AOF 持久化；SCF 环境变量 `STUCK_REDIS_URL` 已配。线上探针（`article-probe-baseline`/`__probe__`）实测 `report` → 空闲 ≥150s 等实例回收 → `top` 仍 `count:1`，**跨冷启动 / 跨实例 / 跨设备共享已生效**。整条「社区飞轮」叙事从 story 变 fact——个人中心「我的贡献」社区数字变真、答主视角跨设备卡点报告变真。不动前端、不动体验。

**所有创意升级动作全部完成。再无待办。**

### 7.4 一句话结论

> **全部完成。** C1 持久化已上线，数据可跨实例跨设备共享；配合已落地的首屏对比图 + 个人中心「我的贡献」+ 答主视角 + 真实知乎长文 + HTTPS + 全链路 CORS，整条「困惑数据 = 知乎空白」叙事从首屏到个人视角**完全闭环、眼见为实**。

---

> 本方案替代并整合：`问题清单.md`、`真实痛点改造方案.md`、`项目复查报告_2026-09-12.md`、`官方要求对照与功能增减建议.md`、`黑客松作品_剩余问题.md`、`知伴_黑客松冲刺计划书.md`、`知伴_定位与落地推广方案.md`、`演示视频脚本.md`。保留 `产品说明文档.md` 作为提交必交件。
